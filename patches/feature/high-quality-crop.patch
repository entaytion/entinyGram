From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.su>
Date: Thu, 2 Jul 2026 19:52:25 +0400
Subject: high-quality photo cropping in PhotoViewer (crop *before* downscaling)

---
 .../telegram/messenger/MediaController.java   | 28 +++++++++++-
 .../java/org/telegram/ui/PhotoViewer.java     | 44 ++++++++++++++-----
 2 files changed, 58 insertions(+), 14 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/MediaController.java b/TMessagesProj/src/main/java/org/telegram/messenger/MediaController.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/MediaController.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/MediaController.java
@@ -488,6 +488,7 @@ public class MediaController implements AudioManager.OnAudioFocusChangeListener,
         public String paintPath;
         public String croppedPaintPath;
         public String fullPaintPath;
+        public String inu_editSourcePath;
 
         public boolean isLivePhoto() {
             return this instanceof PhotoEntry && ((PhotoEntry) this).isLivePhoto();
@@ -522,6 +523,10 @@ public class MediaController implements AudioManager.OnAudioFocusChangeListener,
         }
 
         public void reset() {
+            if (inu_editSourcePath != null) {
+                new File(inu_editSourcePath).delete();
+                inu_editSourcePath = null;
+            }
             caption = null;
             coverPath = null;
             coverPhoto = null;
@@ -554,6 +559,7 @@ public class MediaController implements AudioManager.OnAudioFocusChangeListener,
             paintPath = state.paintPath;
             croppedPaintPath = state.croppedPaintPath;
             fullPaintPath = state.fullPaintPath;
+            inu_editSourcePath = state.inu_editSourcePath;
 
             entities = state.entities;
             savedFilterState = state.savedFilterState;
@@ -776,12 +782,30 @@ public class MediaController implements AudioManager.OnAudioFocusChangeListener,
                     new File(croppedPaintPath).delete();
                 } catch (Exception ignore) {}
             }
+            if (inu_editSourcePath != null) {
+                try {
+                    new File(inu_editSourcePath).delete();
+                } catch (Exception ignore) {}
+            }
         }
 
         public void rebuildPhoto(boolean highQuality) {
-            final Pair<Integer, Integer> orientation = AndroidUtilities.getImageOrientation(filterPath != null ? filterPath : path);
+            final String sourcePath = desu.inugram.helpers.media.PhotoViewerHelper.getEditSourcePath(this);
+            final Pair<Integer, Integer> orientation = AndroidUtilities.getImageOrientation(sourcePath);
             final Bitmap.CompressFormat compressFormat = Bitmap.CompressFormat.JPEG;
-            final Bitmap bitmap = StoryEntry.getScaledBitmap(opts -> BitmapFactory.decodeFile(filterPath != null ? filterPath : path, opts), AndroidUtilities.getPhotoSize(highQuality), AndroidUtilities.getPhotoSize(highQuality), false, true);
+            if (highQuality && fullPaintPath == null && cropState != null && !isVideo) {
+                Bitmap hq = desu.inugram.helpers.media.PhotoViewerHelper.renderHighQualityCrop(this);
+                if (hq != null) {
+                    if (imagePath != null) {
+                        new File(imagePath).delete(); imagePath = null;
+                    }
+                    TLRPC.PhotoSize hqSize = ImageLoader.scaleAndSaveImage(hq, compressFormat, AndroidUtilities.getPhotoSize(highQuality), AndroidUtilities.getPhotoSize(highQuality), 99, false, 101, 101);
+                    imagePath = FileLoader.getInstance(UserConfig.selectedAccount).getPathToAttach(hqSize, true).toString();
+                    hq.recycle();
+                    return;
+                }
+            }
+            final Bitmap bitmap = StoryEntry.getScaledBitmap(opts -> BitmapFactory.decodeFile(sourcePath, opts), AndroidUtilities.getPhotoSize(highQuality), AndroidUtilities.getPhotoSize(highQuality), false, true);
             if (imagePath != null) {
                 new File(imagePath).delete(); imagePath = null;
             }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/PhotoViewer.java b/TMessagesProj/src/main/java/org/telegram/ui/PhotoViewer.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/PhotoViewer.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/PhotoViewer.java
@@ -11480,7 +11480,12 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
             if (entry.cropState == null) {
                 return currentEditMode != EDIT_MODE_CROP;
             }
-            currentCroppedBitmap = createCroppedBitmap(bitmap, entry.cropState, orientation, true);
+            if (!isCurrentVideo) {
+                currentCroppedBitmap = desu.inugram.helpers.media.PhotoViewerHelper.renderHighQualityCrop(entry);
+            }
+            if (currentCroppedBitmap == null) {
+                currentCroppedBitmap = createCroppedBitmap(bitmap, entry.cropState, orientation, true);
+            }
             if (currentCroppedBitmap == null) {
                 currentCroppedBitmap = desu.inugram.helpers.media.PhotoViewerHelper.rebakeCroppedBitmap(entry);
                 if (currentCroppedBitmap == null) {
@@ -11497,6 +11502,9 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
             }
             hasChanged = photoPaintView.hasChanges();
             bitmap = photoPaintView.getBitmap(entities, paintThumbBitmap);
+            if (bitmap == null) {
+                return false;
+            }
             stickers = photoPaintView.getMasks();
 
             photoPaintView.onCleanupEntities();
@@ -11554,11 +11562,12 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                     Bitmap croppedPaintBitmap = createCroppedBitmap(paintBitmap, entry.cropState, null, false);
 
                     if (!isCurrentVideo || entry.isLivePhoto()) {
+                        int editSize = AndroidUtilities.getPhotoSize(true);
                         if (hasAnimatedMediaEntities()) {
-                            TLRPC.PhotoSize size = ImageLoader.scaleAndSaveImage(croppedBitmap, getCompressFormat(), AndroidUtilities.getPhotoSize(), AndroidUtilities.getPhotoSize(), 87, false, 101, 101);
+                            TLRPC.PhotoSize size = ImageLoader.scaleAndSaveImage(croppedBitmap, getCompressFormat(), editSize, editSize, 99, false, 101, 101);
                             entry.imagePath = currentImagePath = FileLoader.getInstance(currentAccount).getPathToAttach(size, true).toString();
                         } else {
-                            mergeImages(entry.imagePath = getTempFileAbsolutePath(), null, croppedPaintBitmap, croppedBitmap, AndroidUtilities.getPhotoSize(), true);
+                            mergeImages(entry.imagePath = getTempFileAbsolutePath(), null, croppedPaintBitmap, croppedBitmap, editSize, true);
                         }
                     }
                     mergeImages(entry.thumbPath = getTempFileAbsolutePath(), null, croppedPaintBitmap, croppedBitmap, thumbSize, true);
@@ -11570,7 +11579,8 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                     }
                 } else {
                     if (!isCurrentVideo || entry.isLivePhoto()) {
-                        TLRPC.PhotoSize size = ImageLoader.scaleAndSaveImage(croppedBitmap, getCompressFormat(), AndroidUtilities.getPhotoSize(), AndroidUtilities.getPhotoSize(), 87, false, 101, 101);
+                        int editSize = AndroidUtilities.getPhotoSize(true);
+                        TLRPC.PhotoSize size = ImageLoader.scaleAndSaveImage(croppedBitmap, getCompressFormat(), editSize, editSize, 99, false, 101, 101);
                         entry.imagePath = currentImagePath = FileLoader.getInstance(currentAccount).getPathToAttach(size, true).toString();
                     }
                     TLRPC.PhotoSize size = ImageLoader.scaleAndSaveImage(croppedBitmap, thumbSize, thumbSize, 70, false, 101, 101);
@@ -11590,7 +11600,8 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
             Bitmap b = entry.cropState != null ? createCroppedBitmap(bitmap, entry.cropState, null, true) : bitmap;
             if (entry.paintPath == null) {
                 if (!isCurrentVideo || entry.isLivePhoto()) {
-                    size = ImageLoader.scaleAndSaveImage(b, getCompressFormat(), AndroidUtilities.getPhotoSize(), AndroidUtilities.getPhotoSize(), 87, false, 101, 101);
+                    int editSize = AndroidUtilities.getPhotoSize(true);
+                    size = ImageLoader.scaleAndSaveImage(b, getCompressFormat(), editSize, editSize, 99, false, 101, 101);
                     entry.imagePath = currentImagePath = FileLoader.getInstance(currentAccount).getPathToAttach(size, true).toString();
                 }
                 size = ImageLoader.scaleAndSaveImage(b, getCompressFormat(), thumbSize, thumbSize, 83, false, 101, 101);
@@ -11614,11 +11625,12 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                     recyclePaint = false;
                 }
                 if (!isCurrentVideo || entry.isLivePhoto()) {
+                    int editSize = AndroidUtilities.getPhotoSize(true);
                     if (hasAnimatedMediaEntities()) {
-                        size = ImageLoader.scaleAndSaveImage(b, getCompressFormat(), AndroidUtilities.getPhotoSize(), AndroidUtilities.getPhotoSize(), 87, false, 101, 101);
+                        size = ImageLoader.scaleAndSaveImage(b, getCompressFormat(), editSize, editSize, 99, false, 101, 101);
                         entry.imagePath = currentImagePath = FileLoader.getInstance(currentAccount).getPathToAttach(size, true).toString();
                     } else {
-                        mergeImages(entry.imagePath = getTempFileAbsolutePath(), path, paintBitmap, b, AndroidUtilities.getPhotoSize(), true);
+                        mergeImages(entry.imagePath = getTempFileAbsolutePath(), path, paintBitmap, b, editSize, true);
                     }
                 }
                 mergeImages(entry.thumbPath = getTempFileAbsolutePath(), path, paintBitmap, b, thumbSize, true);
@@ -11732,11 +11744,12 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                 }
             }
             if (!isCurrentVideo || entry.isLivePhoto()) {
+                int editSize = AndroidUtilities.getPhotoSize(true);
                 if (hasAnimatedMediaEntities()) {
-                    size = ImageLoader.scaleAndSaveImage(croppedBitmap, getCompressFormat(), AndroidUtilities.getPhotoSize(), AndroidUtilities.getPhotoSize(), 87, false, 101, 101);
+                    size = ImageLoader.scaleAndSaveImage(croppedBitmap, getCompressFormat(), editSize, editSize, 99, false, 101, 101);
                     entry.imagePath = currentImagePath = FileLoader.getInstance(currentAccount).getPathToAttach(size, true).toString();
                 } else {
-                    mergeImages(entry.imagePath = getTempFileAbsolutePath(), null, croppedBitmap, paintBitmap, AndroidUtilities.getPhotoSize(), false);
+                    mergeImages(entry.imagePath = getTempFileAbsolutePath(), null, croppedBitmap, paintBitmap, editSize, false);
                 }
             }
             mergeImages(entry.thumbPath = getTempFileAbsolutePath(), null, croppedBitmap, paintBitmap, thumbSize, false);
@@ -12014,6 +12027,12 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         if (currentEditMode == mode || (isCurrentVideo && photoProgressViews[0].backgroundState != 3) && !isCurrentVideo && (centerImage.getBitmap() == null || photoProgressViews[0].backgroundState != -1) || changeModeAnimation != null || imageMoveAnimation != null || isCaptionOpen()) {
             return;
         }
+        if (!isCurrentVideo && currentEditMode == EDIT_MODE_NONE && mode != EDIT_MODE_NONE && currentIndex >= 0 && currentIndex < imagesArrLocals.size()) {
+            Object object = imagesArrLocals.get(currentIndex);
+            if (object instanceof MediaController.MediaEditState) {
+                desu.inugram.helpers.media.PhotoViewerHelper.ensureEditSourceSnapshot((MediaController.MediaEditState) object);
+            }
+        }
         if (placeProvider != null && (currentEditMode == EDIT_MODE_NONE || mode == EDIT_MODE_NONE)) {
             placeProvider.onEditModeChanged(mode != EDIT_MODE_NONE);
         }
@@ -12587,7 +12606,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                 photoFilterView = new PhotoFilterView(parentActivity, videoTextureView != null ? (VideoEditTextureView) videoTextureView : null, bitmap, bitmapMask, orientation, state, isCurrentVideo ? null : paintingOverlay, hasFaces, videoTextureView == null && (editState.cropState != null && editState.cropState.mirrored || cropTransform.isMirrored()), true, null, resourcesProvider);
                 containerView.addView(photoFilterView, LayoutHelper.createFrame(LayoutHelper.MATCH_PARENT, LayoutHelper.MATCH_PARENT));
                 photoFilterView.getDoneTextView().setOnClickListener(v -> {
-                    applyCurrentEditMode();
+                    if (!applyCurrentEditMode()) return;
                     switchToEditMode(EDIT_MODE_NONE);
                 });
                 photoFilterView.getCancelTextView().setOnClickListener(v -> {
@@ -13073,7 +13092,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
             containerView.addView(photoPaintView.getView(), LayoutHelper.createFrame(LayoutHelper.MATCH_PARENT, LayoutHelper.MATCH_PARENT));
             photoPaintView.setOnDoneButtonClickedListener(() -> {
                 savedState = null;
-                applyCurrentEditMode();
+                if (!applyCurrentEditMode()) return;
                 switchToEditMode(EDIT_MODE_NONE);
             });
             photoPaintView.getCancelView().setOnClickListener(v -> closePaintMode());
@@ -16776,7 +16795,8 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                             imageReceiver.setOrientation(photoEntry.orientation, photoEntry.invert, false);
                             path = photoEntry.path;
                         }
-                        filter = String.format(Locale.US, "%d_%d", size, size);
+                        int editSize = (int) (AndroidUtilities.getPhotoSize(true) / AndroidUtilities.density);
+                        filter = String.format(Locale.US, "%d_%d", editSize, editSize);
                     }
                 } else if (object instanceof TLRPC.BotInlineResult) {
                     cacheType = 1;
