From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.pet>
Date: Wed, 26 Aug 2026 22:51:36 +0300
Subject: Play call audio as media on Bluetooth to avoid the low-quality HFP profile

---
 .../telegram/messenger/voip/VoIPService.java  | 37 +++++++++++++++----
 .../java/org/telegram/ui/VoIPFragment.java    |  4 +-
 2 files changed, 31 insertions(+), 10 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/voip/VoIPService.java b/TMessagesProj/src/main/java/org/telegram/messenger/voip/VoIPService.java
index 0000000..0000000 100755
--- a/TMessagesProj/src/main/java/org/telegram/messenger/voip/VoIPService.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/voip/VoIPService.java
@@ -492,7 +492,7 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 				if (BuildVars.LOGS_ENABLED) {
 					FileLog.e("Bluetooth SCO state updated: " + state);
 				}
-				if (state == AudioManager.SCO_AUDIO_STATE_DISCONNECTED && isBtHeadsetConnected) {
+				if (state == AudioManager.SCO_AUDIO_STATE_DISCONNECTED && isBtHeadsetConnected && !desu.inugram.helpers.CallAudioHelper.isActive()) {
 					if (!btAdapter.isEnabled() || !PermissionRequest.hasPermission(Manifest.permission.BLUETOOTH_CONNECT) || btAdapter.getProfileConnectionState(BluetoothProfile.HEADSET) != BluetoothProfile.STATE_CONNECTED) {
 						updateBluetoothHeadsetState(false);
 						return;
@@ -3862,6 +3862,13 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 					break;
 			}
 		} else if (audioConfigured && !USE_CONNECTION_SERVICE) {
+			if (desu.inugram.helpers.CallAudioHelper.handleSetAudioOutput(this, which)) {
+				audioRouteToSet = desu.inugram.helpers.CallAudioHelper.getCurrentRoute();
+				for (StateListener l : stateListeners) {
+					l.onAudioSettingsChanged();
+				}
+				return;
+			}
 			switch (which) {
 				case 2:
 					if (!bluetoothScoActive) {
@@ -3927,6 +3934,9 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 			int route = systemCallConnection.getCallAudioState().getRoute();
 			return hasEarpiece() ? route == CallAudioState.ROUTE_SPEAKER : route == CallAudioState.ROUTE_BLUETOOTH;
 		} else if (audioConfigured && !USE_CONNECTION_SERVICE) {
+			if (desu.inugram.helpers.CallAudioHelper.isActive()) {
+				return desu.inugram.helpers.CallAudioHelper.getCurrentRoute() == AUDIO_ROUTE_SPEAKER;
+			}
 			AudioManager am = (AudioManager) getSystemService(AUDIO_SERVICE);
 			VoipAudioManager vam = VoipAudioManager.get();
 			return hasEarpiece() ? vam.isSpeakerphoneOn() : am.isBluetoothScoOn();
@@ -3950,6 +3960,9 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 			return audioRouteToSet;
 		}
 		if (audioConfigured) {
+			if (desu.inugram.helpers.CallAudioHelper.isActive()) {
+				return desu.inugram.helpers.CallAudioHelper.getCurrentRoute();
+			}
 			AudioManager am = (AudioManager) getSystemService(AUDIO_SERVICE);
 			VoipAudioManager vam = VoipAudioManager.get();
 			if (am.isBluetoothScoOn()) {
@@ -4762,11 +4775,12 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 	}
 
 	private void loadResources() {
+		final boolean inu_mediaAudio = desu.inugram.helpers.CallAudioHelper.evaluate(this);
 		if (Build.VERSION.SDK_INT >= 21) {
 			WebRtcAudioTrack.setAudioTrackUsageAttribute(AudioAttributes.USAGE_VOICE_COMMUNICATION);
 		}
 		Utilities.globalQueue.postRunnable(() -> {
-			soundPool = new SoundPool(1, AudioManager.STREAM_VOICE_CALL, 0);
+			soundPool = new SoundPool(1, inu_mediaAudio ? AudioManager.STREAM_MUSIC : AudioManager.STREAM_VOICE_CALL, 0);
 			spConnectingId = soundPool.load(this, R.raw.voip_connecting, 1);
 			spRingbackID = soundPool.load(this, R.raw.voip_ringback, 1);
 			spFailedID = soundPool.load(this, R.raw.voip_failed, 1);
@@ -4827,10 +4841,11 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 		if (BuildVars.LOGS_ENABLED) {
 			FileLog.d("configureDeviceForCall, route to set = " + audioRouteToSet);
 		}
+		final boolean inu_mediaAudio = desu.inugram.helpers.CallAudioHelper.onConfigureCall(this);
 
 		if (Build.VERSION.SDK_INT >= 21) {
-			WebRtcAudioTrack.setAudioTrackUsageAttribute(hasRtmpStream() ? AudioAttributes.USAGE_MEDIA : AudioAttributes.USAGE_VOICE_COMMUNICATION);
-			WebRtcAudioTrack.setAudioStreamType(hasRtmpStream() ? AudioManager.USE_DEFAULT_STREAM_TYPE : AudioManager.STREAM_VOICE_CALL);
+			WebRtcAudioTrack.setAudioTrackUsageAttribute(hasRtmpStream() || inu_mediaAudio ? AudioAttributes.USAGE_MEDIA : AudioAttributes.USAGE_VOICE_COMMUNICATION);
+			WebRtcAudioTrack.setAudioStreamType(hasRtmpStream() || inu_mediaAudio ? AudioManager.USE_DEFAULT_STREAM_TYPE : AudioManager.STREAM_VOICE_CALL);
 		}
 
 		needPlayEndSound = true;
@@ -4849,7 +4864,7 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 						return;
 					}
 
-					am.setMode(AudioManager.MODE_IN_COMMUNICATION);
+					am.setMode(inu_mediaAudio ? AudioManager.MODE_NORMAL : AudioManager.MODE_IN_COMMUNICATION);
 				} catch (Exception e) {
 					FileLog.e(e);
 				}
@@ -4857,7 +4872,10 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 					int focusResult = am.requestAudioFocus(VoIPService.this, AudioManager.STREAM_VOICE_CALL, AudioManager.AUDIOFOCUS_GAIN_TRANSIENT);
 					hasAudioFocus = focusResult == AudioManager.AUDIOFOCUS_REQUEST_GRANTED;
 					final VoipAudioManager vam = VoipAudioManager.get();
-					if (isBluetoothHeadsetConnected() && hasEarpiece()) {
+					if (inu_mediaAudio) {
+						am.setBluetoothScoOn(false);
+						audioRouteToSet = AUDIO_ROUTE_BLUETOOTH;
+					} else if (isBluetoothHeadsetConnected() && hasEarpiece()) {
 						switch (audioRouteToSet) {
 							case AUDIO_ROUTE_BLUETOOTH:
 								if (!bluetoothScoActive) {
@@ -5007,7 +5025,7 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 				if (BuildVars.LOGS_ENABLED) {
 					FileLog.d("SCO already active, setting audio routing");
 				}
-				if (!hasRtmpStream()) {
+				if (!hasRtmpStream() && !desu.inugram.helpers.CallAudioHelper.isActive()) {
 					am.setSpeakerphoneOn(false);
 					am.setBluetoothScoOn(true);
 				}
@@ -5015,7 +5033,7 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 				if (BuildVars.LOGS_ENABLED) {
 					FileLog.d("startBluetoothSco");
 				}
-				if (!hasRtmpStream()) {
+				if (!hasRtmpStream() && !desu.inugram.helpers.CallAudioHelper.isActive()) {
 					needSwitchToBluetoothAfterScoActivates = true;
 					AndroidUtilities.runOnUIThread(() -> {
 						try {
@@ -5504,6 +5522,9 @@ public class VoIPService extends Service implements SensorEventListener, AudioMa
 	}
 
 	public boolean isBluetoothOn() {
+		if (desu.inugram.helpers.CallAudioHelper.isActive()) {
+			return desu.inugram.helpers.CallAudioHelper.getCurrentRoute() == AUDIO_ROUTE_BLUETOOTH;
+		}
 		final AudioManager am = (AudioManager) getSystemService(AUDIO_SERVICE);
 		return am.isBluetoothScoOn();
 	}
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/VoIPFragment.java b/TMessagesProj/src/main/java/org/telegram/ui/VoIPFragment.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/VoIPFragment.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/VoIPFragment.java
@@ -2768,7 +2768,7 @@ public class VoIPFragment implements
         final int icon;
         if (service.isBluetoothOn()) {
             icon = R.drawable.calls_bluetooth;
-        } else if (VoipAudioManager.get().isSpeakerphoneOn()) {
+        } else if (desu.inugram.helpers.CallAudioHelper.isActive() ? service.isSpeakerphoneOn() : VoipAudioManager.get().isSpeakerphoneOn()) {
             icon = R.drawable.calls_speaker;
         } else {
             if (service.isHeadsetPlugged()) {
@@ -2792,7 +2792,7 @@ public class VoIPFragment implements
         if (service.isBluetoothOn()) {
             selectedSpeaker = 2;
             bottomButton.setType(VoIpSwitchLayout.Type.BLUETOOTH, false);
-        } else if (vam.isSpeakerphoneOn()) {
+        } else if (desu.inugram.helpers.CallAudioHelper.isActive() ? service.isSpeakerphoneOn() : vam.isSpeakerphoneOn()) {
             selectedSpeaker = 0;
             bottomButton.setType(VoIpSwitchLayout.Type.SPEAKER, true);
         } else {
