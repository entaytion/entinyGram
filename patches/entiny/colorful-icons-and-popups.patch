From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Oleksii Kulinich <entaytion@gmail.com>
Date: Wed, 5 Aug 2026 10:20:40 +0300
Subject: feat(ui): adapt colorful icons, Monet theme transitions, dialog inputs, and popups

---
 .../telegram/ui/Adapters/ContactsAdapter.java |  4 +-
 .../java/org/telegram/ui/Cells/TextCell.java  | 58 ++++++++++++++-----
 .../ui/Components/IconBackgroundColors.java   |  3 +-
 .../telegram/ui/Components/ItemOptions.java   |  4 +-
 .../org/telegram/ui/SettingsActivity.java     |  2 +-
 5 files changed, 51 insertions(+), 20 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Adapters/ContactsAdapter.java b/TMessagesProj/src/main/java/org/telegram/ui/Adapters/ContactsAdapter.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Adapters/ContactsAdapter.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Adapters/ContactsAdapter.java
@@ -562,9 +562,9 @@ public class ContactsAdapter extends RecyclerListView.SectionsAdapter {
                 if (section == 0) {
                     if (needPhonebook) {
                         if (position == 0) {
-                            textCell.setTextAndValueAndColorfulIcon(getString(R.string.InviteFriends), "", false, R.drawable.settings_invite, 0xFF1CA5ED, 0xFF1488E1, false);
+                            textCell.setTextAndValueAndColorfulIcon(getString(R.string.InviteFriends), "", false, R.drawable.settings_invite, 0xFF1CA5ED, 0xFF1488E1, false, true);
                         } else if (position == 1) {
-                            textCell.setTextAndValueAndColorfulIcon(getString(R.string.RecentCalls), "", false, R.drawable.settings_calls, 0xFF55CA47, 0xFF27B434, false);
+                            textCell.setTextAndValueAndColorfulIcon(getString(R.string.RecentCalls), "", false, R.drawable.settings_calls, 0xFF55CA47, 0xFF27B434, false, true);
                         }
                     } else if (isAdmin) {
                         if (isChannel) {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/TextCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/TextCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/TextCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/TextCell.java
@@ -62,6 +62,7 @@ public class TextCell extends FrameLayout {
     public int heightDp = desu.inugram.helpers.theme.M3SectionsHelper.isEnabled() ? 56 : 50;
     public int imageLeft = 16;
     public int inu_m3IconSize = 22;
+    private boolean forceStockColorfulIcon;
     private boolean inDialogs;
     private boolean prioritizeTitleOverValue;
     private Theme.ResourcesProvider resourcesProvider;
@@ -203,20 +204,27 @@ public class TextCell extends FrameLayout {
             subtitleView.measure(MeasureSpec.makeMeasureSpec(width - dp(71 + leftPadding) - valueWidth, MeasureSpec.AT_MOST), MeasureSpec.makeMeasureSpec(dp(20), MeasureSpec.EXACTLY));
         }
         if (imageView.getVisibility() == VISIBLE) {
-            if (desu.inugram.helpers.theme.M3SectionsHelper.isEnabled()) {
-                imageView.setScaleType(ImageView.ScaleType.FIT_CENTER);
-                if (imageView.getBackground() instanceof SettingsActivity.SettingCell.Background) {
-                    imageView.setPadding(dp(7), dp(7), dp(7), dp(7));
-                    final int iconMeasureSpec = MeasureSpec.makeMeasureSpec(dp(36), MeasureSpec.EXACTLY);
-                    imageView.measure(iconMeasureSpec, iconMeasureSpec);
+            if (!forceStockColorfulIcon && desu.inugram.helpers.theme.M3SectionsHelper.isCircleIconsEnabled()) {
+                final boolean isColorful = imageView.getBackground() != null;
+                final int targetSize = isColorful ? dp(36) : dp(inu_m3IconSize);
+                if (isColorful) {
+                    int p = dp(6);
+                    imageView.setPadding(p, p, p, p);
                 } else {
                     imageView.setPadding(0, 0, 0, 0);
-                    final boolean autoSizeLottie = inu_m3IconSize == 22 && imageView.getDrawable() instanceof RLottieDrawable;
-                    final int iconMeasureSpec = MeasureSpec.makeMeasureSpec(dp(autoSizeLottie ? 48 : inu_m3IconSize), autoSizeLottie ? MeasureSpec.AT_MOST : MeasureSpec.EXACTLY);
-                    imageView.measure(iconMeasureSpec, iconMeasureSpec);
                 }
+                imageView.setScaleType(ImageView.ScaleType.FIT_CENTER);
+                final boolean autoSizeLottie = !isColorful && inu_m3IconSize == 22 && imageView.getDrawable() instanceof RLottieDrawable;
+                final int iconMeasureSpec = MeasureSpec.makeMeasureSpec(autoSizeLottie ? dp(48) : targetSize, autoSizeLottie ? MeasureSpec.AT_MOST : MeasureSpec.EXACTLY);
+                imageView.measure(iconMeasureSpec, iconMeasureSpec);
+            } else if (imageView.getBackground() != null) {
+                // Colorful rows in non-M3 modes render the stock gradient square at the settings
+                // size (28dp) with the 24dp glyph, so every list icon matches the settings screen.
+                imageView.setScaleType(ImageView.ScaleType.FIT_CENTER);
+                final int tileSpec = MeasureSpec.makeMeasureSpec(dp(28), MeasureSpec.EXACTLY);
+                imageView.measure(tileSpec, tileSpec);
             } else {
-            imageView.measure(MeasureSpec.makeMeasureSpec(width, MeasureSpec.AT_MOST), MeasureSpec.makeMeasureSpec(height, MeasureSpec.AT_MOST));
+                imageView.measure(MeasureSpec.makeMeasureSpec(width, MeasureSpec.AT_MOST), MeasureSpec.makeMeasureSpec(height, MeasureSpec.AT_MOST));
             }
         }
         if (valueImageView.getVisibility() == VISIBLE) {
@@ -500,6 +508,10 @@ public class TextCell extends FrameLayout {
     }
 
     public void setTextAndValueAndColorfulIcon(String text, CharSequence value, boolean animated, int resId, int colorTop, int colorBottom, boolean divider) {
+        setTextAndValueAndColorfulIcon(text, value, animated, resId, colorTop, colorBottom, divider, false);
+    }
+
+    public void setTextAndValueAndColorfulIcon(String text, CharSequence value, boolean animated, int resId, int colorTop, int colorBottom, boolean divider, boolean forceStock) {
         imageLeft = 16;
         offsetFromImage = getOffsetFromImage(false);
         textView.setText(text);
@@ -507,7 +519,7 @@ public class TextCell extends FrameLayout {
         valueTextView.setText(value == null ? "" : TextUtils.ellipsize(valueText = value, valueTextView.getPaint(), AndroidUtilities.displaySize.x / 2.5f, TextUtils.TruncateAt.END), animated);
         valueTextView.setVisibility(VISIBLE);
         valueSpoilersTextView.setVisibility(GONE);
-        setColorfulIcon(colorTop, colorBottom, resId);
+        setColorfulIcon(colorTop, colorBottom, resId, forceStock);
         valueImageView.setVisibility(GONE);
         needDivider = divider;
         setWillNotDraw(!needDivider);
@@ -639,19 +651,37 @@ public class TextCell extends FrameLayout {
     }
 
     public void setColorfulIcon(int colorTop, int colorBottom, int resId) {
+        setColorfulIcon(colorTop, colorBottom, resId, false);
+    }
+
+    public void setColorfulIcon(int colorTop, int colorBottom, int resId, boolean forceStock) {
         offsetFromImage = getOffsetFromImage(true);
         imageView.setVisibility(VISIBLE);
-        imageView.setPadding(dp(2), dp(2), dp(2), dp(2));
+        forceStockColorfulIcon = forceStock;
+        final boolean isM3 = !forceStock && desu.inugram.helpers.theme.M3SectionsHelper.isCircleIconsEnabled();
+        // Match the settings screen exactly: M3 round tiles are a 36dp circle with a 24dp glyph
+        // (6dp inset); every non-M3 mode is a 28dp stock gradient square with a 24dp glyph (2dp inset).
+        final int iconPadding = isM3 ? dp(6) : dp(2);
+        imageView.setPadding(iconPadding, iconPadding, iconPadding, iconPadding);
+        imageView.setScaleType(ImageView.ScaleType.FIT_CENTER);
         imageView.setTranslationX(dp(LocaleController.isRTL ? 0 : -3));
         imageView.setImageResource(resId);
-        imageView.setColorFilter(new PorterDuffColorFilter(Color.WHITE, PorterDuff.Mode.SRC_IN));
+
+        // Stock Telegram look in every non-M3 mode: white glyph on the top→bottom gradient tile
+        // with a subtle border on dark themes. No per-theme "quasi-monet" flat squares.
+        if (!isM3) {
+            imageView.setColorFilter(new PorterDuffColorFilter(Color.WHITE, PorterDuff.Mode.SRC_IN));
+        }
 
         final boolean border = resourcesProvider != null ? resourcesProvider.isDark() : Theme.isCurrentThemeDark();
         SettingsActivity.SettingCell.Background drawable = new SettingsActivity.SettingCell.Background();
         drawable.setColor(colorTop, colorBottom);
+        if (isM3) {
+            drawable.inu_monetColor = desu.inugram.helpers.theme.M3SectionsHelper.circleColor(colorTop, colorBottom);
+            imageView.setColorFilter(new PorterDuffColorFilter(desu.inugram.helpers.theme.M3SectionsHelper.iconColor(colorTop, colorBottom), PorterDuff.Mode.SRC_IN));
+        }
         drawable.setDrawBorder(border);
         imageView.setBackground(drawable);
-        desu.inugram.helpers.theme.M3SectionsHelper.applyTextCellColorfulIcon(imageView, colorTop, colorBottom, drawable);
     }
 
     public void setTextAndCheck(CharSequence text, boolean checked, boolean divider) {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/IconBackgroundColors.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/IconBackgroundColors.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/IconBackgroundColors.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/IconBackgroundColors.java
@@ -13,7 +13,8 @@ public enum IconBackgroundColors {
     RED(0xFFF45255, 0xFFDF3955),
     CYAN(0xFF32C0CE, 0xFF1D9CC6),
     PURPLE(0xFFC46EF4, 0xFF9F55DF),
-    GRAY(0xFF8699AA, 0xFF6E8397);
+    GRAY(0xFF8699AA, 0xFF6E8397),
+    ENTINY(0xFFF20C3C, 0xFFF20C3C);
 
     public final int top;
     public final int bottom;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/ItemOptions.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/ItemOptions.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/ItemOptions.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/ItemOptions.java
@@ -217,7 +217,7 @@ public class ItemOptions {
         this.resourcesProvider = fragment.getResourceProvider();
         this.context = fragment.getContext();
         this.scrimView = scrimView;
-        this.dimAlpha = AndroidUtilities.computePerceivedBrightness(Theme.getColor(Theme.key_windowBackgroundWhite, resourcesProvider)) > .705 ? 0x66 : 0x33;
+        this.dimAlpha = AndroidUtilities.computePerceivedBrightness(Theme.getColor(Theme.key_windowBackgroundWhite, resourcesProvider)) > .705 ? 0x26 : 0x33;
         this.swipeback = swipeback;
         this.useScrollView = useScrollView;
         this.shownFromBottom = shownFromBottom;
@@ -238,7 +238,7 @@ public class ItemOptions {
         this.resourcesProvider = resourcesProvider;
         this.context = container.getContext();
         this.scrimView = scrimView;
-        this.dimAlpha = AndroidUtilities.computePerceivedBrightness(Theme.getColor(Theme.key_windowBackgroundWhite, resourcesProvider)) > .705 ? 0x66 : 0x33;
+        this.dimAlpha = AndroidUtilities.computePerceivedBrightness(Theme.getColor(Theme.key_windowBackgroundWhite, resourcesProvider)) > .705 ? 0x26 : 0x33;
         this.swipeback = swipeback;
         this.shownFromBottom = shownFromBottom;
         this.useScrollView = useScrollView;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/SettingsActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/SettingsActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/SettingsActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/SettingsActivity.java
@@ -695,7 +695,7 @@ public class SettingsActivity extends BaseFragment implements NotificationCenter
         }
 
         if (!desu.inugram.helpers.security.ParanoiaHelper.shouldHideSettings()) {
-            items.add(SettingCell.Factory.of(99, IconBackgroundColors.PURPLE.top, IconBackgroundColors.PURPLE.bottom, R.drawable.icon_settings_inu, getString(R.string.InuSettings), getString(R.string.InuSettingsInfo)));
+            items.add(SettingCell.Factory.of(99, IconBackgroundColors.ENTINY.top, IconBackgroundColors.ENTINY.bottom, R.drawable.icon_settings_inu, getString(R.string.InuSettings), getString(R.string.InuSettingsInfo)));
             items.add(UItem.asShadow(null));
         }
 
