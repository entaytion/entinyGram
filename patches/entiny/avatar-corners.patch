From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Oleksii Kulinich <entaytion@gmail.com>
Date: Fri, 21 Aug 2026 22:55:44 +0300
Subject: Add customizable avatar corner radius

- avatar corner radius is now driven by a 0-28dp slider instead of the hardcoded fully-round shape
- applies to ImageReceiver-drawn avatars (dialog list, chat header, forum topics), AvatarDrawable placeholder backgrounds, and the profile screen expanded avatar (ProfileActivity/ProfileActivity2)
- optional "unified corner radius" mode applies the same radius to forum avatars instead of their own multiplier
---
 .../org/telegram/messenger/ImageReceiver.java | 16 ++++++--
 .../ui/Components/AvatarDrawable.java         | 10 ++++-
 .../Components/Premium/FeaturesPageView.java  | 40 +++++++++++++++----
 .../java/org/telegram/ui/ProfileActivity.java | 18 +++++++++
 .../org/telegram/ui/ProfileActivity2.java     |  5 ++-
 .../ui/Stories/ProfileStoriesView.java        | 10 +++--
 .../telegram/ui/Stories/StoriesUtilities.java | 11 +++--
 .../ui/Stories/recorder/StoryRecorder.java    |  3 +-
 8 files changed, 90 insertions(+), 23 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/ImageReceiver.java b/TMessagesProj/src/main/java/org/telegram/messenger/ImageReceiver.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/ImageReceiver.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/ImageReceiver.java
@@ -326,6 +326,8 @@ public class ImageReceiver implements NotificationCenter.NotificationCenterDeleg
     private final int[] roundRadius = new int[4];
     private int[] emptyRoundRadius;
     private boolean isRoundRect = true;
+    public boolean inu_isAvatarImage = false;
+    public boolean inu_isForumAvatar = false;
     private Object mark;
 
     private Paint roundPaint;
@@ -425,6 +427,8 @@ public class ImageReceiver implements NotificationCenter.NotificationCenterDeleg
     }
 
     public void setForUserOrChat(TLObject object, Drawable avatarDrawable, Object parentObject, boolean animationEnabled, int vectorType, boolean big) {
+        inu_isAvatarImage = true;
+        inu_isForumAvatar = object instanceof TLRPC.Chat && ((TLRPC.Chat) object).forum;
         if (parentObject == null) {
             parentObject = object;
         }
@@ -1398,10 +1402,12 @@ public class ImageReceiver implements NotificationCenter.NotificationCenterDeleg
                         if (isRoundRect && useRoundRadius) {
                             try {
                                 if (canvas != null) {
-                                    if (roundRadius[0] == 0) {
+                                    final int maxR = (int) (Math.min(roundRect.width(), roundRect.height()) / 2f);
+                                    final int inu_r = inu_isAvatarImage ? desu.inugram.helpers.theme.AvatarShapeHelper.resolveAvatarRadius(roundRadius[0], maxR, inu_isForumAvatar) : roundRadius[0];
+                                    if (inu_r == 0) {
                                         canvas.drawRect(roundRect, roundPaint);
                                     } else {
-                                        canvas.drawRoundRect(roundRect, roundRadius[0], roundRadius[0], roundPaint);
+                                        canvas.drawRoundRect(roundRect, inu_r, inu_r, roundPaint);
                                     }
                                 }
                             } catch (Exception e) {
@@ -1508,7 +1514,9 @@ public class ImageReceiver implements NotificationCenter.NotificationCenterDeleg
                         if (isRoundRect && useRoundRadius) {
                             try {
                                 if (canvas != null) {
-                                    if (roundRadius[0] == 0) {
+                                    final int maxR = (int) (Math.min(roundRect.width(), roundRect.height()) / 2f);
+                                    final int inu_r = inu_isAvatarImage ? desu.inugram.helpers.theme.AvatarShapeHelper.resolveAvatarRadius(roundRadius[0], maxR, inu_isForumAvatar) : roundRadius[0];
+                                    if (inu_r == 0) {
                                         if (reactionLastFrame) {
                                             AndroidUtilities.rectTmp.set(roundRect);
                                             AndroidUtilities.rectTmp.inset(-(drawRegion.width() * ReactionLastFrame.LAST_FRAME_SCALE - drawRegion.width()) / 2f, -(drawRegion.height() * ReactionLastFrame.LAST_FRAME_SCALE - drawRegion.height()) / 2f);
@@ -1517,7 +1525,7 @@ public class ImageReceiver implements NotificationCenter.NotificationCenterDeleg
                                             canvas.drawRect(roundRect, roundPaint);
                                         }
                                     } else {
-                                        canvas.drawRoundRect(roundRect, roundRadius[0], roundRadius[0], roundPaint);
+                                        canvas.drawRoundRect(roundRect, inu_r, inu_r, roundPaint);
                                     }
                                 }
                             } catch (Exception e) {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/AvatarDrawable.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/AvatarDrawable.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/AvatarDrawable.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/AvatarDrawable.java
@@ -588,7 +588,15 @@ public class AvatarDrawable extends Drawable {
                 canvas.save();
                 canvas.rotate(-45, size / 2.0f, size / 2.0f);
             }
-            if (roundRadius > 0) {
+            if (desu.inugram.InuConfig.AVATAR_CORNERS.getValue() < 28.0f) {
+                int r = desu.inugram.helpers.theme.AvatarShapeHelper.resolveRadius(roundRadius > 0 ? roundRadius : size / 2, size / 2);
+                AndroidUtilities.rectTmp.set(0, 0, size, size);
+                if (r <= 0) {
+                    canvas.drawRect(AndroidUtilities.rectTmp, backgroundPaint);
+                } else {
+                    canvas.drawRoundRect(AndroidUtilities.rectTmp, r, r, backgroundPaint);
+                }
+            } else if (roundRadius > 0) {
                 AndroidUtilities.rectTmp.set(0, 0, size, size);
                 canvas.drawRoundRect(AndroidUtilities.rectTmp, roundRadius, roundRadius, backgroundPaint);
             } else {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/Premium/FeaturesPageView.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/Premium/FeaturesPageView.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/Premium/FeaturesPageView.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/Premium/FeaturesPageView.java
@@ -7,6 +7,8 @@ import android.graphics.Bitmap;
 import android.graphics.Canvas;
 import android.graphics.LinearGradient;
 import android.graphics.Paint;
+import android.graphics.Path;
+import android.graphics.PathMeasure;
 import android.graphics.PorterDuff;
 import android.graphics.PorterDuffColorFilter;
 import android.graphics.Shader;
@@ -371,14 +373,36 @@ public class FeaturesPageView extends BaseListPageView {
                 AndroidUtilities.rectTmp.inset(-dp(5), -dp(5));
                 gradientTools.setBounds(AndroidUtilities.rectTmp);
                 int storiesCount = 7;
-                float step = 360 / (float) storiesCount;
-                int gapLen = 5;
-                for (int i = 0; i < storiesCount; i++) {
-                    float startAngle = step * i - 90;
-                    float endAngle = startAngle + step;
-                    startAngle += gapLen;
-                    endAngle -= gapLen;
-                    canvas.drawArc(AndroidUtilities.rectTmp, startAngle, (endAngle - startAngle), false, gradientTools.paint);
+                float r = AndroidUtilities.rectTmp.height() / 2f;
+                if (desu.inugram.InuConfig.AVATAR_CORNERS.getValue() < 28.0f) {
+                    r = desu.inugram.helpers.theme.AvatarShapeHelper.resolveRadius((int) r, (int) r);
+                }
+                if (r < AndroidUtilities.rectTmp.height() / 2f) {
+                    Path path = new Path();
+                    path.addRoundRect(AndroidUtilities.rectTmp, r, r, Path.Direction.CW);
+                    PathMeasure pm = new PathMeasure(path, false);
+                    float len = pm.getLength();
+                    float step = len / (float) storiesCount;
+                    float gap = dp(3.5f);
+                    Path segment = new Path();
+                    for (int i = 0; i < storiesCount; i++) {
+                        float start = step * i + gap / 2f;
+                        float end = step * (i + 1) - gap / 2f;
+                        segment.reset();
+                        pm.getSegment(start, end, segment, true);
+                        segment.rLineTo(0, 0);
+                        canvas.drawPath(segment, gradientTools.paint);
+                    }
+                } else {
+                    float step = 360 / (float) storiesCount;
+                    int gapLen = 5;
+                    for (int i = 0; i < storiesCount; i++) {
+                        float startAngle = step * i - 90;
+                        float endAngle = startAngle + step;
+                        startAngle += gapLen;
+                        endAngle -= gapLen;
+                        canvas.drawArc(AndroidUtilities.rectTmp, startAngle, (endAngle - startAngle), false, gradientTools.paint);
+                    }
                 }
             }
             super.dispatchDraw(canvas);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
@@ -1081,6 +1081,11 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                 AndroidUtilities.rectTmp.set(inset, inset, thisWidth - inset, thisHeight - inset);
                 clipPath.addRoundRect(AndroidUtilities.rectTmp, r, r, Path.Direction.CW);
                 canvas.clipPath(clipPath);
+            } else if (desu.inugram.InuConfig.AVATAR_CORNERS.getValue() <= 0.0f) {
+                clipPath.rewind();
+                AndroidUtilities.rectTmp.set(inset, inset, thisWidth - inset, thisHeight - inset);
+                clipPath.addRect(AndroidUtilities.rectTmp, Path.Direction.CW);
+                canvas.clipPath(clipPath);
             }
 
             canvas.save();
@@ -5405,6 +5410,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
             }
         };
         avatarImage.createBlurEffect(getActionsExtraHeight());
+        avatarImage.getImageReceiver().inu_isAvatarImage = true;
         avatarImage.getImageReceiver().setAllowDecodeSingleFrame(true);
         avatarImage.setRoundRadiusForExpand(getSmallAvatarRoundRadius());
         avatarImage.setPivotX(0);
@@ -6490,6 +6496,18 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     }
 
     private int getSmallAvatarRoundRadius() {
+        if (desu.inugram.InuConfig.AVATAR_CORNERS.getValue() < 28.0f) {
+            boolean isForum = false;
+            if (chatId != 0) {
+                TLRPC.Chat chatLocal = getMessagesController().getChat(chatId);
+                isForum = org.telegram.messenger.ChatObject.isForum(chatLocal);
+            }
+            return desu.inugram.helpers.theme.AvatarShapeHelper.getAvatarRoundRadiusPx(
+                (float) dp(100),
+                isForum,
+                needInsetForStories()
+            );
+        }
         if (isTopic) {
             return 0;
         }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity2.java b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity2.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity2.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity2.java
@@ -954,6 +954,7 @@ public class ProfileActivity2 extends BaseFragment implements
         public AvatarImage(Context context, Theme.ResourcesProvider resourcesProvider) {
             super(context);
             this.resourcesProvider = resourcesProvider;
+            imageReceiver.inu_isAvatarImage = true;
         }
 
         @Override
@@ -990,7 +991,8 @@ public class ProfileActivity2 extends BaseFragment implements
             final float avatarCy =     lerp3(dpf2(28), dpf2(59), expandedHeight / 2.0f, relativeTop);
             final float avatarW =      lerp3(dpf2(42), dpf2(90), getWidth(), relativeTop);
             final float avatarH =      lerp3(dpf2(42), dpf2(90), expandedHeight, relativeTop);
-            final float avatarRadius = lerp3(avatarW / 2.0f, avatarW / 2.0f, 0, relativeTop);
+            final float baseRadius =   desu.inugram.InuConfig.AVATAR_CORNERS.getValue() < 28.0f ? desu.inugram.helpers.theme.AvatarShapeHelper.resolveRadius((int) (avatarW / 2.0f)) : avatarW / 2.0f;
+            final float avatarRadius = lerp3(baseRadius, baseRadius, 0, relativeTop);
 
             avatarRect.set(
                 avatarCx - avatarW / 2.0f,
@@ -999,6 +1001,7 @@ public class ProfileActivity2 extends BaseFragment implements
                 avatarCy + avatarH / 2.0f
             );
 
+            avatarDrawable.setRoundRadius((int) avatarRadius);
             imageReceiver.setImageCoords(avatarRect);
             imageReceiver.setRoundRadius((int) avatarRadius);
             imageReceiver.draw(canvas);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Stories/ProfileStoriesView.java b/TMessagesProj/src/main/java/org/telegram/ui/Stories/ProfileStoriesView.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Stories/ProfileStoriesView.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Stories/ProfileStoriesView.java
@@ -609,8 +609,9 @@ public class ProfileStoriesView extends View implements NotificationCenter.Notif
                 paint.setStrokeWidth(AndroidUtilities.dp(2));
                 paint.setAlpha((int) (255 * segmentsAlpha));
                 boolean isForum = ChatObject.isForum(UserConfig.selectedAccount, dialogId);
-                if (isForum) {
-                    float r = rect2.height() * 0.32f;
+                boolean inu_treatForum = isForum && !desu.inugram.InuConfig.UNIFIED_AVATAR_RADIUS.getValue();
+                if (inu_treatForum || desu.inugram.InuConfig.AVATAR_CORNERS.getValue() < 28.0f) {
+                    float r = inu_treatForum ? rect2.height() * 0.32f : desu.inugram.helpers.theme.AvatarShapeHelper.resolveRadius((int) (rect2.height() / 2f), (int) (rect2.height() / 2f));
                     canvas.drawRoundRect(rect2, r, r, paint);
                 } else {
                     canvas.drawCircle(rect2.centerX(), rect2.centerY(), rect2.width() / 2f, paint);
@@ -902,8 +903,9 @@ public class ProfileStoriesView extends View implements NotificationCenter.Notif
 
     private void drawArc(Canvas canvas, RectF oval, float startAngle, float sweepAngle, boolean useCenter, Paint paint) {
         boolean isForum = ChatObject.isForum(UserConfig.selectedAccount, dialogId);
-        if (isForum) {
-            float r = oval.height() * 0.32f;
+        boolean inu_treatForum = isForum && !desu.inugram.InuConfig.UNIFIED_AVATAR_RADIUS.getValue();
+        if (inu_treatForum || desu.inugram.InuConfig.AVATAR_CORNERS.getValue() < 28.0f) {
+            float r = inu_treatForum ? oval.height() * 0.32f : desu.inugram.helpers.theme.AvatarShapeHelper.resolveRadius((int) (oval.height() / 2f), (int) (oval.height() / 2f));
             if (Math.abs(sweepAngle) == 360) {
                 canvas.drawRoundRect(oval, r, r, paint);
                 return;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Stories/StoriesUtilities.java b/TMessagesProj/src/main/java/org/telegram/ui/Stories/StoriesUtilities.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Stories/StoriesUtilities.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Stories/StoriesUtilities.java
@@ -662,10 +662,12 @@ public class StoriesUtilities {
     private static final RectF forumRect = new RectF();
 
     private static void drawCircleInternal(Canvas canvas, View view, AvatarStoryParams params, Paint paint, boolean isForum) {
-        if (isForum) {
+        boolean inu_treatForum = isForum && !desu.inugram.InuConfig.UNIFIED_AVATAR_RADIUS.getValue();
+        if (inu_treatForum || desu.inugram.InuConfig.AVATAR_CORNERS.getValue() < 28.0f) {
+            float r = inu_treatForum ? dp(18) : desu.inugram.helpers.theme.AvatarShapeHelper.resolveRadius((int) (rectTmp.height() / 2f), (int) (rectTmp.height() / 2f));
             forumRect.set(rectTmp);
             forumRect.inset(dp(0.5f), dp(0.5f));
-            canvas.drawRoundRect(forumRect, dp(18), dp(18), paint);
+            canvas.drawRoundRect(forumRect, r, r, paint);
             return;
         }
         if (params.progressToArc == 0) {
@@ -681,8 +683,9 @@ public class StoriesUtilities {
     private static final Path forumSegmentPath = new Path();
 
     private static void drawSegment(Canvas canvas, RectF rectTmp, Paint paint, float startAngle, float endAngle, AvatarStoryParams params, boolean isForum) {
-        if (isForum) {
-            float r = rectTmp.height() * 0.32f;
+        boolean inu_treatForum = isForum && !desu.inugram.InuConfig.UNIFIED_AVATAR_RADIUS.getValue();
+        if (inu_treatForum || desu.inugram.InuConfig.AVATAR_CORNERS.getValue() < 28.0f) {
+            float r = inu_treatForum ? rectTmp.height() * 0.32f : desu.inugram.helpers.theme.AvatarShapeHelper.resolveRadius((int) (rectTmp.height() / 2f), (int) (rectTmp.height() / 2f));
             float rotateAngle = (((int)(startAngle)) / 90) * 90 + 90;
             float pathAngleStart = -199 + rotateAngle;
             float percentFrom = (startAngle - pathAngleStart) / 360;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Stories/recorder/StoryRecorder.java b/TMessagesProj/src/main/java/org/telegram/ui/Stories/recorder/StoryRecorder.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Stories/recorder/StoryRecorder.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Stories/recorder/StoryRecorder.java
@@ -286,7 +286,8 @@ public class StoryRecorder implements NotificationCenter.NotificationCenterDeleg
             }
             float scale = ((View)avatarImage.getParent()).getScaleX();
             final float size = avatarImage.getImageReceiver().getImageWidth() * scale;
-            final float rounding = isForum ? size * 0.32f : size;
+            final boolean inu_treatForum = isForum && !desu.inugram.InuConfig.UNIFIED_AVATAR_RADIUS.getValue();
+            final float rounding = inu_treatForum ? size * 0.32f : size;
             SourceView src = new SourceView() {
                 @Override
                 protected void show(boolean sent) {
