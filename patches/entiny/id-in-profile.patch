From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.su>
Date: Sun, 12 Apr 2026 18:08:10 +0300
Subject: show peer id in profile

---
 .../java/org/telegram/ui/ProfileActivity.java | 31 ++++++++++++++++++-
 1 file changed, 30 insertions(+), 1 deletion(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
@@ -344,6 +344,7 @@ import java.util.zip.ZipEntry;
 import java.util.zip.ZipOutputStream;
 
 import desu.inugram.helpers.dialogs.MainTabsHelper;
+import desu.inugram.helpers.profile.ProfileHelper;
 import me.vkryl.android.animator.BoolAnimator;
 import me.vkryl.core.reference.ReferenceList;
 
@@ -619,6 +620,8 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     public int birthdayRow;
     private int setUsernameRow;
     private int bioRow;
+    public int inu_idRow;
+    public int inu_regDateRow;
     private int phoneSuggestionSectionRow;
     private int graceSuggestionRow;
     private int graceSuggestionSectionRow;
@@ -4525,6 +4528,10 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                         showDialog(new CommunitySheet(this, user.linked_community_id));
                     }
                 }
+            } else if (position == inu_idRow) {
+                ProfileHelper.onIdRowClick(ProfileActivity.this, listView.getClipBackground(view), view, userId, chatId, getAccountInstance());
+            } else if (position == inu_regDateRow) {
+                ProfileHelper.onRegDateRowClick(ProfileActivity.this, listView.getClipBackground(view), view, userId, chatId, getAccountInstance());
             } else if (position == locationRow) {
                 if (chatInfo.location instanceof TLRPC.TL_channelLocation) {
                     LocationActivity fragment = new LocationActivity(LocationActivity.LOCATION_TYPE_GROUP_VIEW);
@@ -5079,6 +5086,12 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                 } else if (position == noteRow) {
                     editNotes(view, position);
                     return true;
+                } else if (position == inu_idRow) {
+                    ProfileHelper.onIdRowClick(ProfileActivity.this, listView.getClipBackground(view), view, userId, chatId, getAccountInstance());
+                    return true;
+                } else if (position == inu_regDateRow) {
+                    ProfileHelper.onRegDateRowClick(ProfileActivity.this, listView.getClipBackground(view), view, userId, chatId, getAccountInstance());
+                    return true;
                 } else {
                     if (editRow(view, position)) return true;
                     return processOnClickOrPress(position, view, view.getWidth() / 2f, (int) (view.getHeight() * .75f));
@@ -10527,6 +10540,8 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         birthdayRow = -1;
         setUsernameRow = -1;
         bioRow = -1;
+        inu_idRow = -1;
+        inu_regDateRow = -1;
         channelRow = -1;
         channelDividerRow = -1;
         phoneSuggestionSectionRow = -1;
@@ -10691,6 +10706,8 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                 numberSectionRow = rowCount++;
                 numberRow = rowCount++;
                 setUsernameRow = rowCount++;
+                if (ProfileHelper.shouldShowIdRow()) inu_idRow = rowCount++;
+                if (ProfileHelper.shouldShowRegDateRow(userId, currentChat)) inu_regDateRow = rowCount++;
                 bioRow = rowCount++;
 
                 settingsSectionRow = rowCount++;
@@ -10783,6 +10800,8 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                 if (user != null && username != null) {
                     usernameRow = rowCount++;
                 }
+                if (ProfileHelper.shouldShowIdRow()) inu_idRow = rowCount++;
+                if (ProfileHelper.shouldShowRegDateRow(userId, currentChat)) inu_regDateRow = rowCount++;
                 if (userInfo != null) {
                     if (userInfo.birthday != null) {
                         birthdayRow = rowCount++;
@@ -10962,6 +10981,8 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                     usernameRow = rowCount++;
                 }
             }
+            if (ProfileHelper.shouldShowIdRow()) inu_idRow = rowCount++;
+            if (ProfileHelper.shouldShowRegDateRow(userId, currentChat)) inu_regDateRow = rowCount++;
             if (emptyRow < 0 && emptyRow2 < 0) {
                 if (hasMusic || peerColor != null || actionsView == null) {
                     emptyRow2 = rowCount++;
@@ -13608,6 +13629,12 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                         }
                         isFragmentPhoneNumber = phoneNumber != null && phoneNumber.matches("888\\d{8}");
                         detailCell.setTextAndValue(text, LocaleController.getString(isFragmentPhoneNumber ? R.string.AnonymousNumber : R.string.PhoneMobile), false);
+                    } else if (position == inu_idRow) {
+                        detailCell.setTextAndValue(ProfileHelper.formatId(userId, chatId != 0 ? getMessagesController().getChat(chatId) : null), LocaleController.getString(R.string.InuProfileId), false);
+                    } else if (position == inu_regDateRow) {
+                        TLRPC.User u = userId != 0 ? getMessagesController().getUser(userId) : null;
+                        TLRPC.Chat c = chatId != 0 ? getMessagesController().getChat(chatId) : null;
+                        detailCell.setTextAndValue(ProfileHelper.getRegDateValue(userId, c), ProfileHelper.getRegDateSubtitle(userId, c, u), false);
                     } else if (position == noteRow) {
                         if (userInfo == null || userInfo.note == null) return;
                         TLRPC.TL_textWithEntities note = userInfo.note;
@@ -14429,7 +14456,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
             if (position == infoHeaderRow || position == membersHeaderRow || position == settingsSectionRow2 ||
                     position == numberSectionRow || position == helpHeaderRow || position == debugHeaderRow || position == botPermissionsHeader) {
                 return VIEW_TYPE_HEADER;
-            } else if (position == phoneRow || position == locationRow || position == numberRow || position == birthdayRow) {
+            } else if (position == phoneRow || position == locationRow || position == numberRow || position == birthdayRow || position == inu_idRow || position == inu_regDateRow) {
                 return VIEW_TYPE_TEXT_DETAIL;
             } else if (position == usernameRow || position == setUsernameRow) {
                 return VIEW_TYPE_TEXT_DETAIL_MULTILINE;
@@ -15889,6 +15916,8 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
             put(++pointer, bizHoursRow, sparseIntArray);
             put(++pointer, bizLocationRow, sparseIntArray);
             put(++pointer, birthdayRow, sparseIntArray);
+            put(++pointer, inu_idRow, sparseIntArray);
+            put(++pointer, inu_regDateRow, sparseIntArray);
             put(++pointer, channelRow, sparseIntArray);
             put(++pointer, botStarsBalanceRow, sparseIntArray);
             put(++pointer, botTonBalanceRow, sparseIntArray);
