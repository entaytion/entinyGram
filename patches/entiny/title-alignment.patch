From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Oleksii Kulinich <entaytion@gmail.com>
Date: Sun, 26 Jul 2026 01:30:43 +0300
Subject: Center chat title/subtitle alignment; fix subtitle EXACTLY-width mismatch

---
 .../org/telegram/ui/ActionBar/ActionBar.java  | 156 ++++++++++++++----
 .../telegram/ui/ActionBar/SimpleTextView.java | 113 +++++++------
 .../telegram/ui/Cells/ChatMessageCell.java    |  13 +-
 .../org/telegram/ui/Cells/DialogCell.java     |  10 +-
 .../telegram/ui/Cells/ManageChatUserCell.java |   8 +-
 .../telegram/ui/Cells/ProfileSearchCell.java  |   2 +-
 .../java/org/telegram/ui/Cells/UserCell.java  |   4 +
 .../java/org/telegram/ui/ChatActivity.java    |   2 +-
 .../BottomSheetWithRecyclerListView.java      |   1 +
 .../ui/Components/ChatAvatarContainer.java    | 153 +++++++++++++----
 .../java/org/telegram/ui/DialogsActivity.java |   4 +
 .../java/org/telegram/ui/ProfileActivity.java |  14 +-
 .../ui/Stories/DialogStoriesCell.java         |  14 +-
 13 files changed, 365 insertions(+), 129 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java b/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/ActionBar.java
@@ -93,6 +93,7 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
     private BlurredBackgroundDrawable glassDrawable;
     private Drawable glassDrawableBack;
     private Drawable glassDrawableMenu;
+    private Drawable glassDrawableAvatar;
     public boolean inu_nonIsland;
     private INavigationLayout.BackButtonState backButtonState = INavigationLayout.BackButtonState.BACK;
     public ImageView backButtonImageView;
@@ -198,6 +199,10 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
     private boolean glassModeIsForum;
 
     private ChatAvatarContainer chatAvatarContainer;
+    public int inu_glassTitleLeft;
+    public int inu_glassTitleRight;
+    public int inu_glassTop;
+    public int inu_glassBottom;
 
     public void setGlassOnlyBack() {
         glassOnlyBack = true;
@@ -462,8 +467,11 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
         if (subtitleTextView != null) {
             return;
         }
+        if (inu_shouldCenterActionBarTitle()) {
+            isCenterTitle = true;
+        }
         subtitleTextView = new SimpleTextView(getContext());
-        subtitleTextView.setGravity(Gravity.LEFT);
+        subtitleTextView.setGravity(isCenterTitle ? Gravity.CENTER : Gravity.LEFT);
         subtitleTextView.setVisibility(GONE);
         subtitleTextView.setTextColor(getThemedColor(Theme.key_actionBarDefaultSubtitle));
         addView(subtitleTextView, 0, LayoutHelper.createFrame(LayoutHelper.WRAP_CONTENT, LayoutHelper.WRAP_CONTENT, Gravity.LEFT | Gravity.TOP));
@@ -515,6 +523,9 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
         if (titleTextView[i] != null) {
             return;
         }
+        if (inu_shouldCenterActionBarTitle()) {
+            isCenterTitle = true;
+        }
         titleTextView[i] = new SimpleTextView(getContext());
         titleTextView[i].setGravity(isCenterTitle ? Gravity.CENTER : Gravity.LEFT | Gravity.CENTER_VERTICAL);
         if (titleColorToSet != 0) {
@@ -547,6 +558,37 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
         }
     }
 
+    private boolean inu_forceDisableCenterTitle;
+
+    private boolean inu_shouldCenterActionBarTitle() {
+        return !inu_forceDisableCenterTitle && desu.inugram.helpers.InuUtils.shouldCenterTitle(parentFragment);
+    }
+
+    private void syncInuCenteredGravities() {
+        if (titleTextView != null) {
+            for (int a = 0; a < titleTextView.length; a++) {
+                if (titleTextView[a] != null) {
+                    titleTextView[a].setGravity(isCenterTitle ? Gravity.CENTER : Gravity.LEFT | Gravity.CENTER_VERTICAL);
+                }
+            }
+        }
+        if (subtitleTextView != null) {
+            subtitleTextView.setGravity(isCenterTitle ? Gravity.CENTER : Gravity.LEFT);
+        }
+        if (additionalSubtitleTextView != null) {
+            additionalSubtitleTextView.setGravity(isCenterTitle ? Gravity.CENTER : Gravity.LEFT);
+        }
+    }
+
+    public void inu_setForceDisableCenterTitle(boolean value) {
+        if (inu_forceDisableCenterTitle == value) {
+            return;
+        }
+        inu_forceDisableCenterTitle = value;
+        syncInuCenteredGravities();
+        requestLayout();
+    }
+
     public void setTitleRightMargin(int value) {
         titleRightMargin = value;
     }
@@ -1379,6 +1421,13 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
         int actionBarHeight = getCurrentActionBarHeight();
         int actionBarHeightSpec = MeasureSpec.makeMeasureSpec(actionBarHeight, MeasureSpec.EXACTLY);
 
+        // inu: sync the centered-title flag BEFORE anything is measured so that
+        // widthWrapContent and gravity always agree with the current screen state.
+        // Gravities are re-asserted unconditionally on every pass - a stale LEFT
+        // gravity combined with the symmetric layout throws the title off-center.
+        isCenterTitle = inu_shouldCenterActionBarTitle();
+        syncInuCenteredGravities();
+
         ignoreLayoutRequest = true;
         if (actionModeTop != null) {
             FrameLayout.LayoutParams layoutParams = (FrameLayout.LayoutParams) actionModeTop.getLayoutParams();
@@ -1398,6 +1447,9 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
         } else {
             textLeft = dp(AndroidUtilities.isTablet() ? 26 : 18);
         }
+        if (avatarSearchImageView != null && avatarSearchImageView.getVisibility() != GONE) {
+            textLeft = Math.max(textLeft, dp(56 + 8) + avatarSearchImageView.getMeasuredWidth() + dp(8));
+        }
         // textLeft += additionalTextLeft;
 
         if (menu != null && menu.getVisibility() != GONE) {
@@ -1455,6 +1507,9 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
                 }
 
                 if (titleTextView[i] != null && titleTextView[i].getVisibility() != GONE) {
+                    // inu: keep stock full-width measuring here - the symmetric placement in
+                    // onLayout plus CENTER gravity does the centering; wrapping the view to the
+                    // text width breaks SimpleTextView's internal offsetX and throws it right.
                     titleTextView[i].measure(MeasureSpec.makeMeasureSpec(availableWidth, MeasureSpec.AT_MOST), MeasureSpec.makeMeasureSpec(dp(24) + titleTextView[i].getPaddingTop() + titleTextView[i].getPaddingBottom(), MeasureSpec.AT_MOST));
                     if (centerScale) {
                         CharSequence text = titleTextView[i].getText();
@@ -1502,12 +1557,16 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
 
     @Override
     protected void onLayout(boolean changed, int left, int top, int right, int bottom) {
+        updateGlassBounds(right - left, bottom - top);
         int additionalTop = occupyStatusBar ? AndroidUtilities.statusBarHeight : 0;
         if (prevWidth != getMeasuredWidth()) {
             prevWidth = getMeasuredWidth();
             checkAvatarContainerWidth(animatorAvatarContainerWidth.isAnimating());
         }
 
+        // inu: centered-title state is synced at the top of onMeasure so that
+        // measure (widthWrapContent) and layout always agree with each other.
+
         int textLeft;
         if (backButtonImageView != null && backButtonImageView.getVisibility() != GONE) {
             backButtonImageView.layout(0, additionalTop, backButtonImageView.getMeasuredWidth(), additionalTop + backButtonImageView.getMeasuredHeight());
@@ -1515,6 +1574,9 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
         } else {
             textLeft = glassMode ? dp(24) : dp(AndroidUtilities.isTablet() ? 26 : 18);
         }
+        if (avatarSearchImageView != null && avatarSearchImageView.getVisibility() != GONE) {
+            textLeft = Math.max(textLeft, dp(56 + 8) + avatarSearchImageView.getMeasuredWidth() + dp(8));
+        }
         textLeft += additionalTextLeft;
 
         if (menu != null && menu.getVisibility() != GONE) {
@@ -1534,7 +1596,16 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
                         textTop = (getCurrentActionBarHeight() - titleTextView[i].getTextHeight()) / 2;
                     }
                 }
-                titleTextView[i].layout(textLeft, additionalTop + textTop - titleTextView[i].getPaddingTop(), textLeft + titleTextView[i].getMeasuredWidth(), additionalTop + textTop + titleTextView[i].getTextHeight() - titleTextView[i].getPaddingTop() + titleTextView[i].getPaddingBottom());
+                if (isCenterTitle) {
+                    // Port of exteraless/exteraGram centered-title layout: place the view symmetrically
+                    // around the action bar's horizontal center so its CENTER gravity lands exactly there.
+                    // The old formula offset the view origin to the *text's* center while the view itself
+                    // is measured at full available width, double-shifting the visible text right.
+                    int halfWidth = titleTextView[i].getMeasuredWidth() / 2;
+                    titleTextView[i].layout((right - left) / 2 - halfWidth, additionalTop + textTop - titleTextView[i].getPaddingTop(), (right - left) / 2 + halfWidth, additionalTop + textTop + titleTextView[i].getTextHeight() - titleTextView[i].getPaddingTop() + titleTextView[i].getPaddingBottom());
+                } else {
+                    titleTextView[i].layout(textLeft, additionalTop + textTop - titleTextView[i].getPaddingTop(), textLeft + titleTextView[i].getMeasuredWidth(), additionalTop + textTop + titleTextView[i].getTextHeight() - titleTextView[i].getPaddingTop() + titleTextView[i].getPaddingBottom());
+                }
             }
         }
         if (additionalSubTitleOverlayContainer != null) {
@@ -1543,12 +1614,22 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
         }
         if (subtitleTextView != null && subtitleTextView.getVisibility() != GONE) {
             int textTop = getCurrentActionBarHeight() / 2 + (getCurrentActionBarHeight() / 2 - subtitleTextView.getTextHeight()) / 2 - dp(2);
-            subtitleTextView.layout(textLeft, additionalTop + textTop, textLeft + subtitleTextView.getMeasuredWidth(), additionalTop + textTop + subtitleTextView.getTextHeight());
+            if (isCenterTitle) {
+                // See the centered-title comment above: symmetric placement around the center.
+                subtitleTextView.layout((right - left) / 2 - subtitleTextView.getMeasuredWidth() / 2, additionalTop + textTop, (right - left) / 2 + subtitleTextView.getMeasuredWidth() / 2, additionalTop + textTop + subtitleTextView.getTextHeight());
+            } else {
+                subtitleTextView.layout(textLeft, additionalTop + textTop, textLeft + subtitleTextView.getMeasuredWidth(), additionalTop + textTop + subtitleTextView.getTextHeight());
+            }
         }
 
         if (additionalSubtitleTextView != null && additionalSubtitleTextView.getVisibility() != GONE) {
             int textTop = getCurrentActionBarHeight() / 2 + (getCurrentActionBarHeight() / 2 - additionalSubtitleTextView.getTextHeight()) / 2 - dp(!AndroidUtilities.isTablet() && getResources().getConfiguration().orientation == Configuration.ORIENTATION_LANDSCAPE ? 1 : 1);
-            additionalSubtitleTextView.layout(textLeft, additionalTop + textTop, textLeft + additionalSubtitleTextView.getMeasuredWidth(), additionalTop + textTop + additionalSubtitleTextView.getTextHeight());
+            if (isCenterTitle) {
+                // See the centered-title comment above: symmetric placement around the center.
+                additionalSubtitleTextView.layout((right - left) / 2 - additionalSubtitleTextView.getMeasuredWidth() / 2, additionalTop + textTop, (right - left) / 2 + additionalSubtitleTextView.getMeasuredWidth() / 2, additionalTop + textTop + additionalSubtitleTextView.getTextHeight());
+            } else {
+                additionalSubtitleTextView.layout(textLeft, additionalTop + textTop, textLeft + additionalSubtitleTextView.getMeasuredWidth(), additionalTop + textTop + additionalSubtitleTextView.getTextHeight());
+            }
         }
 
         if (avatarSearchImageView != null) {
@@ -1606,6 +1687,8 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
             }
             child.layout(childLeft, childTop, childLeft + width, childTop + height);
         }
+
+        updateGlassBounds(right - left, bottom - top);
     }
 
     public void onMenuButtonPressed() {
@@ -2097,9 +2180,6 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
 
         final boolean hasAvatar = chatAvatarContainer.hasVisibleAvatar();
         int visualWidth = chatAvatarContainer.getVisualWidth();
-        if (hasAvatar) {
-            visualWidth = Math.max(visualWidth, dp(192));
-        }
 
         final int width = Math.min(getMeasuredWidth() - dp(6 + 46 + 6 + 6 + 46 + 6), visualWidth);
         if (animated) {
@@ -2152,44 +2232,47 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
         }
     }
 
+    public int inu_getMenuWidth() {
+        return hasForcedMenuWidth ? forcedMenuWidth : (int) animatorMenuItemsWidth.getFactor();
+    }
+
+    public void updateGlassBounds(int width, int height) {
+        final int p = dp(6);
+        final int s = dp(46);
+        final int menuWidth = hasForcedMenuWidth ? forcedMenuWidth : (int) animatorMenuItemsWidth.getFactor();
+        final boolean hasBackButton = backButtonImageView != null && backButtonImageView.getVisibility() == View.VISIBLE;
+
+        final int t = height - (getCurrentActionBarHeight() + s) / 2 - p;
+        final int b = t + s + p * 2;
+
+        final int menuWidthWithPadding = menuWidth + (hasForcedMenuWidth ? (menuWidth > 0 ? p : 0) : (int) (p * animatorHasMenuItems.getFloatValue()));
+        final int rightOffset = menuWidthWithPadding;
+        final int leftDefault = hasBackButton ? s + p : 0;
+        final int rightDefault = width - rightOffset;
+
+        inu_glassTitleLeft = leftDefault;
+        inu_glassTitleRight = rightDefault;
+        inu_glassTop = t;
+        inu_glassBottom = b;
+    }
+
     @Override
     protected void dispatchDraw(Canvas canvas) {
+        updateGlassBounds(getWidth(), getHeight());
         final int p = dp(6);
         final int s = dp(46);
 
-        final float actionModeFactor = getActionModeFactor();
         final int menuWidth = hasForcedMenuWidth ? forcedMenuWidth : (int) animatorMenuItemsWidth.getFactor();
-
         final boolean hasBackButton = backButtonImageView != null && backButtonImageView.getVisibility() == View.VISIBLE;
 
-        final int t = getHeight() - (getCurrentActionBarHeight() + s) / 2 - p;
-        final int b = t + s + p * 2;
+        final int t = inu_glassTop;
+        final int b = inu_glassBottom;
 
         if (glassDrawable != null && !glassOnlyBack) {
-            final int menuWidthWithPadding = menuWidth + (hasForcedMenuWidth ? (menuWidth > 0 ? p : 0) : (int) (p * animatorHasMenuItems.getFloatValue()));
-            final int rightOffset = lerp(menuWidthWithPadding, Math.max(menuWidthWithPadding, p + s), chatAvatarContainer == null ? 0f : 1f - animatorAvatarContainerHasAvatar.getFloatValue());
-
-            final int leftDefault = lerp(hasBackButton ? s + p : 0, s + p, chatAvatarContainer == null? 0f : 1f - animatorAvatarContainerHasAvatar.getFloatValue());
-            final int rightDefault = getWidth() - rightOffset;
-            final int widthDefault = rightDefault - leftDefault;
-            final int left, right;
-            if (chatAvatarContainer != null) {
-                final int width = lerp(Math.min(widthDefault, (int) animatorAvatarContainerWidth.getFactor() + p * 2), widthDefault, Math.max(searchFactor, actionModeFactor));
-                left = (rightDefault + leftDefault - width) / 2;
-                right = left + width;
-                chatAvatarContainer.setTranslationX(left
-                    - ((MarginLayoutParams)(chatAvatarContainer.getLayoutParams())).leftMargin
-                    - chatAvatarContainer.getLeftPadding()
-                    + p + dp(3));
-            } else {
-                left = leftDefault;
-                right = rightDefault;
-            }
-
             if (inu_nonIsland) {
                 glassDrawable.setBounds(0, 0, getWidth(), getHeight());
             } else {
-                glassDrawable.setBounds(left, t, right, b);
+                glassDrawable.setBounds(inu_glassTitleLeft, t, inu_glassTitleRight, b);
             }
             glassDrawable.draw(canvas);
         }
@@ -2202,6 +2285,15 @@ public class ActionBar extends FrameLayout implements FactorAnimator.Target, The
             glassDrawableMenu.setAlpha(hasForcedMenuWidth ? 255 : (int) (255 * animatorHasMenuItems.getFloatValue()));
             glassDrawableMenu.draw(canvas);
         }
+        if (glassDrawableBack != null && hasBackButton && !inu_nonIsland) {
+            glassDrawableBack.setBounds(0, t, s + p * 2, b);
+            glassDrawableBack.draw(canvas);
+        }
+        if (glassDrawableMenu != null && menuWidth > 0 && !glassOnlyBack && !inu_nonIsland) {
+            glassDrawableMenu.setBounds(getWidth() - Math.max(s, menuWidth) - p * 2, t, getWidth(), b);
+            glassDrawableMenu.setAlpha(hasForcedMenuWidth ? 255 : (int) (255 * animatorHasMenuItems.getFloatValue()));
+            glassDrawableMenu.draw(canvas);
+        }
 
         if (blurredBackground && actionBarColor != Color.TRANSPARENT) {
             rectTmp.set(0, 0, getMeasuredWidth(), getMeasuredHeight());
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/SimpleTextView.java b/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/SimpleTextView.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/SimpleTextView.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ActionBar/SimpleTextView.java
@@ -98,6 +98,8 @@ public class SimpleTextView extends View implements Drawable.Callback {
     private int textHeight;
     public int rightDrawableX;
     public int rightDrawableY;
+    public int rightDrawable2X;
+    public int rightDrawable2Y;
     private boolean wasLayout;
 
     private boolean leftDrawableOutside, rightDrawableOutside;
@@ -129,6 +131,8 @@ public class SimpleTextView extends View implements Drawable.Callback {
     private boolean rightDrawableHidden;
     private OnClickListener rightDrawableOnClickListener;
     private boolean maybeClick;
+    private OnClickListener rightDrawable2OnClickListener;
+    private boolean maybeClick2;
     private float touchDownX, touchDownY;
 
     private AnimatedEmojiSpan.EmojiGroupedSpans emojiStack;
@@ -339,7 +343,8 @@ public class SimpleTextView extends View implements Drawable.Callback {
             }
 
             if ((gravity & Gravity.HORIZONTAL_GRAVITY_MASK) == Gravity.CENTER_HORIZONTAL) {
-                offsetX = Math.max(0, (width - textWidth) / 2) - (int) layout.getLineLeft(0);
+                int totalContentWidth = textWidth + getSideDrawablesSize();
+                offsetX = Math.max(0, (width - totalContentWidth) / 2) - (int) layout.getLineLeft(0);
             } else if ((gravity & Gravity.HORIZONTAL_GRAVITY_MASK) == Gravity.LEFT) {
                 if (firstLineLayout != null) {
                     offsetX = -(int) firstLineLayout.getLineLeft(0);
@@ -1180,16 +1185,15 @@ public class SimpleTextView extends View implements Drawable.Callback {
     }
 
     public float getExactWidth() {
-        return getPaint().measureText(getText().toString())
+        CharSequence t = getText();
+        return (t != null ? getPaint().measureText(t.toString()) : 0f)
                 + getSideDrawablesSize()
                 - (leftDrawable != null || rightDrawable != null || rightDrawable2 != null ? drawablePadding : 0);
     }
 
     public float getExactWidthIncludeDrawables() {
-        return getExactWidth()
-            + (leftDrawable != null ? leftDrawable.getIntrinsicWidth() : 0)
-            + (rightDrawable != null ? rightDrawable.getIntrinsicWidth() : 0)
-            + (rightDrawable2 != null ? rightDrawable2.getIntrinsicWidth() : 0);
+        CharSequence t = getText();
+        return (t != null ? getPaint().measureText(t.toString()) : 0f) + getSideDrawablesSize();
     }
 
     private void drawLayout(Canvas canvas) {
@@ -1341,14 +1345,6 @@ public class SimpleTextView extends View implements Drawable.Callback {
         return rightDrawableOutside;
     }
 
-    private int rightDrawableX, rightDrawableY;
-    private OnClickListener rightDrawableOnClickListener;
-    private boolean maybeClick;
-
-    private int rightDrawable2X, rightDrawable2Y;
-    private OnClickListener rightDrawable2OnClickListener;
-    private boolean maybeClick2;
-
     public void setRightDrawableOnClick(OnClickListener onClickListener) {
         rightDrawableOnClickListener = onClickListener;
     }
@@ -1359,9 +1355,27 @@ public class SimpleTextView extends View implements Drawable.Callback {
 
     @Override
     public boolean onTouchEvent(MotionEvent event) {
-        if (rightDrawableOnClickListener != null && rightDrawable != null) {
-            AndroidUtilities.rectTmp.set(rightDrawableX - dp(16), rightDrawableY - dp(16), rightDrawableX + dp(16), rightDrawableY + dp(16));
-            if (event.getAction() == MotionEvent.ACTION_DOWN && AndroidUtilities.rectTmp.contains((int) event.getX(), (int) event.getY())) {
+        if (event.getAction() == MotionEvent.ACTION_DOWN) {
+            boolean in1 = false;
+            boolean in2 = false;
+            if (rightDrawableOnClickListener != null && rightDrawable != null) {
+                AndroidUtilities.rectTmp.set(rightDrawableX - dp(16), rightDrawableY - dp(16), rightDrawableX + dp(16), rightDrawableY + dp(16));
+                in1 = AndroidUtilities.rectTmp.contains((int) event.getX(), (int) event.getY());
+            }
+            if (rightDrawable2OnClickListener != null && rightDrawable2 != null) {
+                AndroidUtilities.rectTmp.set(rightDrawable2X - dp(16), rightDrawable2Y - dp(16), rightDrawable2X + dp(16), rightDrawable2Y + dp(16));
+                in2 = AndroidUtilities.rectTmp.contains((int) event.getX(), (int) event.getY());
+            }
+            if (in1 && in2) {
+                float dist1 = Math.abs(event.getX() - rightDrawableX);
+                float dist2 = Math.abs(event.getX() - rightDrawable2X);
+                if (dist1 <= dist2) {
+                    in2 = false;
+                } else {
+                    in1 = false;
+                }
+            }
+            if (in1) {
                 maybeClick = true;
                 touchDownX = event.getX();
                 touchDownY = event.getY();
@@ -1369,28 +1383,7 @@ public class SimpleTextView extends View implements Drawable.Callback {
                 if (rightDrawable instanceof PressableDrawable) {
                     ((PressableDrawable) rightDrawable).setPressed(true);
                 }
-            } else if (event.getAction() == MotionEvent.ACTION_MOVE && maybeClick) {
-                if (Math.abs(event.getX() - touchDownX) >= AndroidUtilities.touchSlop || Math.abs(event.getY() - touchDownY) >= AndroidUtilities.touchSlop) {
-                    maybeClick = false;
-                    getParent().requestDisallowInterceptTouchEvent(false);
-                    if (rightDrawable instanceof PressableDrawable) {
-                        ((PressableDrawable) rightDrawable).setPressed(false);
-                    }
-                }
-            } else if (event.getAction() == MotionEvent.ACTION_UP || event.getAction() == MotionEvent.ACTION_CANCEL) {
-                if (maybeClick && event.getAction() == MotionEvent.ACTION_UP) {
-                    rightDrawableOnClickListener.onClick(this);
-                    if (rightDrawable instanceof PressableDrawable) {
-                        ((PressableDrawable) rightDrawable).setPressed(false);
-                    }
-                }
-                maybeClick = false;
-                getParent().requestDisallowInterceptTouchEvent(false);
-            }
-        }
-        if (rightDrawable2OnClickListener != null && rightDrawable2 != null) {
-            AndroidUtilities.rectTmp.set(rightDrawable2X - dp(16), rightDrawable2Y - dp(16), rightDrawable2X + dp(16), rightDrawable2Y + dp(16));
-            if (event.getAction() == MotionEvent.ACTION_DOWN && AndroidUtilities.rectTmp.contains((int) event.getX(), (int) event.getY())) {
+            } else if (in2) {
                 maybeClick2 = true;
                 touchDownX = event.getX();
                 touchDownY = event.getY();
@@ -1398,24 +1391,38 @@ public class SimpleTextView extends View implements Drawable.Callback {
                 if (rightDrawable2 instanceof PressableDrawable) {
                     ((PressableDrawable) rightDrawable2).setPressed(true);
                 }
-            } else if (event.getAction() == MotionEvent.ACTION_MOVE && maybeClick2) {
-                if (Math.abs(event.getX() - touchDownX) >= AndroidUtilities.touchSlop || Math.abs(event.getY() - touchDownY) >= AndroidUtilities.touchSlop) {
-                    maybeClick2 = false;
-                    getParent().requestDisallowInterceptTouchEvent(false);
-                    if (rightDrawable2 instanceof PressableDrawable) {
-                        ((PressableDrawable) rightDrawable2).setPressed(false);
-                    }
-                }
-            } else if (event.getAction() == MotionEvent.ACTION_UP || event.getAction() == MotionEvent.ACTION_CANCEL) {
-                if (maybeClick2 && event.getAction() == MotionEvent.ACTION_UP) {
-                    rightDrawable2OnClickListener.onClick(this);
-                    if (rightDrawable2 instanceof PressableDrawable) {
-                        ((PressableDrawable) rightDrawable2).setPressed(false);
-                    }
+            }
+        } else if (event.getAction() == MotionEvent.ACTION_MOVE) {
+            if (maybeClick && (Math.abs(event.getX() - touchDownX) >= AndroidUtilities.touchSlop || Math.abs(event.getY() - touchDownY) >= AndroidUtilities.touchSlop)) {
+                maybeClick = false;
+                getParent().requestDisallowInterceptTouchEvent(false);
+                if (rightDrawable instanceof PressableDrawable) {
+                    ((PressableDrawable) rightDrawable).setPressed(false);
                 }
+            }
+            if (maybeClick2 && (Math.abs(event.getX() - touchDownX) >= AndroidUtilities.touchSlop || Math.abs(event.getY() - touchDownY) >= AndroidUtilities.touchSlop)) {
                 maybeClick2 = false;
                 getParent().requestDisallowInterceptTouchEvent(false);
+                if (rightDrawable2 instanceof PressableDrawable) {
+                    ((PressableDrawable) rightDrawable2).setPressed(false);
+                }
+            }
+        } else if (event.getAction() == MotionEvent.ACTION_UP || event.getAction() == MotionEvent.ACTION_CANCEL) {
+            if (maybeClick && event.getAction() == MotionEvent.ACTION_UP) {
+                rightDrawableOnClickListener.onClick(this);
+            }
+            if (maybeClick2 && event.getAction() == MotionEvent.ACTION_UP) {
+                rightDrawable2OnClickListener.onClick(this);
+            }
+            if (rightDrawable instanceof PressableDrawable) {
+                ((PressableDrawable) rightDrawable).setPressed(false);
+            }
+            if (rightDrawable2 instanceof PressableDrawable) {
+                ((PressableDrawable) rightDrawable2).setPressed(false);
             }
+            maybeClick = false;
+            maybeClick2 = false;
+            getParent().requestDisallowInterceptTouchEvent(false);
         }
         return super.onTouchEvent(event) || maybeClick || maybeClick2;
     }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
@@ -22106,9 +22106,16 @@ public class ChatMessageCell extends BaseCell implements SeekBar.SeekBarDelegate
                 currentChat != null ? currentChat.id : 0L
             );
             if (inuBadge != null) {
-                // Shift past the premium/emoji status icon drawn right after the name.
-                int statusWidth = (currentNameStatus != null && currentNameStatus.getIntrinsicWidth() > 0) ? currentNameStatus.getIntrinsicWidth() + dp(4) : (currentNameStatus != null ? dp(20) : 0);
-                int badgeX = (int) (nx + nameLayout.getLineRight(0) + dp(4) + statusWidth);
+                boolean hasNameStatus = currentNameStatusDrawable != null && !currentNameStatusDrawable.isEmpty();
+                int badgeX;
+                if (hasNameStatus) {
+                    // Match the exact anchor used for currentNameStatusDrawable's own bounds above,
+                    // since nameLayoutWidth/viaNameWidth can differ from the actual rendered text
+                    // width (e.g. short names), which would otherwise let the badge overlap it.
+                    badgeX = (int) (Math.abs(nx) + (viaNameWidth > 0 ? viaNameWidth - dp(4 + 28) : nameLayoutWidth) + dp(22) + dp(4));
+                } else {
+                    badgeX = (int) (nx + nameLayout.getLineRight(0) + dp(4));
+                }
                 int badgeY = (int) (ny + (nameLayout.getHeight() - inuBadge.getIntrinsicHeight()) / 2f);
                 setDrawableBounds(inuBadge, badgeX, badgeY);
                 inuBadge.draw(canvas);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/DialogCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/DialogCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/DialogCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/DialogCell.java
@@ -2383,9 +2383,13 @@ public class DialogCell extends BaseCell implements StoriesListPlaceProvider.Ava
         if (drawBotVerified) {
             nameWidth -= dp(21);
         }
-        long inuBadgeUserId = user != null ? user.id : (currentDialogId > 0 ? currentDialogId : 0L);
-        long inuBadgeChatId = chat != null ? chat.id : (currentDialogId < 0 ? currentDialogId : 0L);
-        inuDevBadge = desu.inugram.helpers.DevBadgesHelper.badgeDrawable(getContext(), inuBadgeUserId, inuBadgeChatId);
+        if (!DialogObject.isFolderDialogId(currentDialogId)) {
+            long inuBadgeUserId = user != null ? user.id : (currentDialogId > 0 ? currentDialogId : 0L);
+            long inuBadgeChatId = chat != null ? chat.id : (currentDialogId < 0 ? currentDialogId : 0L);
+            inuDevBadge = desu.inugram.helpers.DevBadgesHelper.badgeDrawable(getContext(), inuBadgeUserId, inuBadgeChatId);
+        } else {
+            inuDevBadge = null;
+        }
         if (inuDevBadge != null) {
             int w = inuDevBadge.getIntrinsicWidth() + dp(4);
             nameWidth -= w;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ManageChatUserCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ManageChatUserCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ManageChatUserCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ManageChatUserCell.java
@@ -282,7 +282,9 @@ public class ManageChatUserCell extends FrameLayout {
                 lastName = newName == null ? UserObject.getUserName(currentUser) : newName;
                 nameTextView.setText(Emoji.replaceEmoji(lastName, nameTextView.getPaint().getFontMetricsInt(), false));
             }
-            desu.inugram.helpers.DevBadgesHelper.applyRightBadge(nameTextView, currentUser.id, 0L, desu.inugram.helpers.DevBadgesHelper.BADGE_SIZE_DP);
+            boolean hasStatus = currentUser.premium || MessagesController.getInstance(currentAccount).isPremiumUser(currentUser) || DialogObject.getEmojiStatusDocumentId(currentUser.emoji_status) != 0;
+            boolean hasVerified = currentUser.verified || currentUser.scam || currentUser.fake;
+            desu.inugram.helpers.DevBadgesHelper.applyRightBadge(nameTextView, currentUser.id, 0L, hasStatus, hasVerified, desu.inugram.helpers.DevBadgesHelper.BADGE_SIZE_DP);
             if (currentStatus != null) {
                 statusTextView.setTextColor(statusColor);
                 statusTextView.setText(currentStatus);
@@ -348,7 +350,9 @@ public class ManageChatUserCell extends FrameLayout {
                 lastName = newName == null ? currentChat.title : newName;
                 nameTextView.setText(lastName);
             }
-            desu.inugram.helpers.DevBadgesHelper.applyRightBadge(nameTextView, 0L, currentChat.id, desu.inugram.helpers.DevBadgesHelper.BADGE_SIZE_DP);
+            boolean hasStatus = DialogObject.getEmojiStatusDocumentId(currentChat.emoji_status) != 0;
+            boolean hasVerified = currentChat.verified || currentChat.scam || currentChat.fake;
+            desu.inugram.helpers.DevBadgesHelper.applyRightBadge(nameTextView, 0L, currentChat.id, hasStatus, hasVerified, desu.inugram.helpers.DevBadgesHelper.BADGE_SIZE_DP);
             if (currentStatus != null) {
                 statusTextView.setTextColor(statusColor);
                 statusTextView.setText(currentStatus);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ProfileSearchCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ProfileSearchCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ProfileSearchCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ProfileSearchCell.java
@@ -986,7 +986,7 @@ public class ProfileSearchCell extends BaseCell implements NotificationCenter.No
             nameLayout.draw(canvas);
             canvas.restore();
 
-            boolean hasStatus = statusDrawable != null && statusDrawable.getIntrinsicWidth() > 0;
+            boolean hasStatus = statusDrawable != null && !statusDrawable.isEmpty();
             float textW = nameLayout.getLineWidth(0);
             if (LocaleController.isRTL) {
                 x = (int) (nameLeft + nameWidth - Math.ceil(textW) - dp(4));
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/UserCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/UserCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/UserCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/UserCell.java
@@ -734,10 +734,14 @@ public class UserCell extends FrameLayout implements NotificationCenter.Notifica
             nameTextView.setRightDrawableTopPadding(0);
         }
         if (currentUser != null) {
+            boolean hasStatus = (MessagesController.getInstance(currentAccount).isPremiumUser(currentUser) && !MessagesController.getInstance(currentAccount).premiumFeaturesBlocked()) || DialogObject.getEmojiStatusDocumentId(currentUser.emoji_status) != 0;
+            boolean hasVerified = currentUser.verified || currentUser.scam || currentUser.fake;
             desu.inugram.helpers.DevBadgesHelper.applyRightBadge(
                 nameTextView,
                 currentUser.id,
                 0L,
+                hasStatus,
+                hasVerified,
                 desu.inugram.helpers.DevBadgesHelper.BADGE_SIZE_DP
             );
         } else {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -4134,7 +4134,7 @@ public class ChatActivity extends BaseFragment implements
         }
         avatarContainer = new ChatAvatarContainer(context, this, currentEncryptedChat != null, themeDelegate) {
             @Override
-            protected boolean onAvatarClick() {
+            public boolean onAvatarClick() {
                 if (currentUser != null && currentUser.linked_community_id != 0) {
                     showDialog(new CommunitySheet(ChatActivity.this, currentUser.linked_community_id));
                     return true;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/BottomSheetWithRecyclerListView.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/BottomSheetWithRecyclerListView.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/BottomSheetWithRecyclerListView.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/BottomSheetWithRecyclerListView.java
@@ -407,6 +407,7 @@ public abstract class BottomSheetWithRecyclerListView extends BottomSheet {
                 }
             });
 
+            actionBar.inu_setForceDisableCenterTitle(true); // inu: sheet titles are left-aligned by design
             containerView.addView(recyclerListView);
             containerView.addView(actionBar, LayoutHelper.createFrame(LayoutHelper.MATCH_PARENT, LayoutHelper.WRAP_CONTENT, 0, 6, 0, 6, 0));
 
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
@@ -270,15 +270,17 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
             });
         }
 
+        boolean centerChatTitle = isCenteredTitle();
+
         titleTextView = new SimpleTextConnectedView(context, titleTextLargerCopyView);
         titleTextView.setEllipsizeByGradient(true);
         titleTextView.setTextColor(getThemedColor(Theme.key_actionBarDefaultTitle));
         titleTextView.setTextSize(18);
-        titleTextView.setGravity(Gravity.LEFT);
+        titleTextView.setGravity(centerChatTitle ? Gravity.CENTER_HORIZONTAL : Gravity.LEFT);
         titleTextView.setTypeface(AndroidUtilities.bold());
         titleTextView.setLeftDrawableTopPadding(-dp(1.3f));
         titleTextView.setCanHideRightDrawable(false);
-        titleTextView.setRightDrawableOutside(true);
+        titleTextView.setRightDrawableOutside(!centerChatTitle);
         titleTextView.setPadding(0, dp(6), 0, dp(12));
         addView(titleTextView);
 
@@ -289,7 +291,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
             animatedSubtitleTextView.setTextColor(getThemedColor(Theme.key_actionBarDefaultSubtitle));
             animatedSubtitleTextView.setTag(Theme.key_actionBarDefaultSubtitle);
             animatedSubtitleTextView.setTextSize(dp(14));
-            animatedSubtitleTextView.setGravity(Gravity.LEFT);
+            animatedSubtitleTextView.setGravity(centerChatTitle ? Gravity.CENTER_HORIZONTAL : Gravity.LEFT);
             animatedSubtitleTextView.setPadding(0, 0, dp(10), 0);
             animatedSubtitleTextView.setTranslationY(-dp(1));
             addView(animatedSubtitleTextView);
@@ -299,7 +301,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
             subtitleTextView.setTextColor(getThemedColor(Theme.key_actionBarDefaultSubtitle));
             subtitleTextView.setTag(Theme.key_actionBarDefaultSubtitle);
             subtitleTextView.setTextSize(14);
-            subtitleTextView.setGravity(Gravity.LEFT);
+            subtitleTextView.setGravity(centerChatTitle ? Gravity.CENTER_HORIZONTAL : Gravity.LEFT);
             subtitleTextView.setPadding(0, 0, dp(10), 0);
             addView(subtitleTextView);
         }
@@ -463,7 +465,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
 
     }
 
-    protected boolean onAvatarClick() {
+    public boolean onAvatarClick() {
         return false;
     }
 
@@ -657,12 +659,39 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
 
     @Override
     protected void onMeasure(int widthMeasureSpec, int heightMeasureSpec) {
-        final int width = MeasureSpec.getSize(widthMeasureSpec);
-        final int availableWidth = width - dp((avatarImageView.getVisibility() == VISIBLE ? 54 : 0) + 16);
+        int width = MeasureSpec.getSize(widthMeasureSpec);
+        boolean centerChatTitle = isCenteredTitle();
+        if (centerChatTitle && actionBar != null) {
+            int targetPill = actionBar.inu_glassTitleRight - actionBar.inu_glassTitleLeft;
+            if (targetPill > 0) {
+                width = Math.min(width, targetPill);
+            }
+        }
+        final int availableWidth;
+        if (centerChatTitle) {
+            // Mirror onLayout's textLeftBound/textRightBound math so the EXACTLY-measured
+            // width used for ellipsizing matches the box the text is actually laid out into —
+            // otherwise long subtitles (e.g. member count + online) get visually clipped
+            // instead of ellipsized because the layout box in onLayout is narrower than this.
+            boolean rightAvatar = isRightAvatar();
+            int avatarW = hasVisibleAvatar() ? dp(avatarSizeInDp) - 2 : 0;
+            int inset = hasVisibleAvatar()
+                    ? (1 + leftPadding + avatarW + dp(4))
+                    : dp(rightAvatar ? (glassMode ? 14 : 10) : (glassMode ? 20 : 16));
+            availableWidth = Math.max(0, width - 2 * inset - rightAvatarPadding);
+        } else {
+            availableWidth = width - dp((hasVisibleAvatar() ? 54 : 0) + (glassMode ? 24 : 16));
+        }
+        float baseSizePx = dp(glassMode ? 17.5f : 18);
+        if (Math.abs(titleTextView.getTextPaint().getTextSize() - baseSizePx) > 0.5f) {
+            titleTextView.setTextSizePx((int) baseSizePx);
+        }
         avatarImageView.measure(MeasureSpec.makeMeasureSpec(dp(avatarSizeInDp) - 2, MeasureSpec.EXACTLY), MeasureSpec.makeMeasureSpec(dp(avatarSizeInDp) - 2, MeasureSpec.EXACTLY));
-        titleTextView.measure(MeasureSpec.makeMeasureSpec(availableWidth, MeasureSpec.AT_MOST), MeasureSpec.makeMeasureSpec(dp(24 + 8), MeasureSpec.AT_MOST));
+        titleTextView.setWidthWrapContent(!centerChatTitle);
+        titleTextView.measure(MeasureSpec.makeMeasureSpec(availableWidth, centerChatTitle ? MeasureSpec.EXACTLY : MeasureSpec.AT_MOST), MeasureSpec.makeMeasureSpec(dp(24 + 8), MeasureSpec.AT_MOST));
         if (subtitleTextView != null) {
-            subtitleTextView.measure(MeasureSpec.makeMeasureSpec(availableWidth, MeasureSpec.AT_MOST), MeasureSpec.makeMeasureSpec(dp(20), MeasureSpec.AT_MOST));
+            subtitleTextView.setWidthWrapContent(!centerChatTitle);
+            subtitleTextView.measure(MeasureSpec.makeMeasureSpec(availableWidth, centerChatTitle ? MeasureSpec.EXACTLY : MeasureSpec.AT_MOST), MeasureSpec.makeMeasureSpec(dp(20), MeasureSpec.AT_MOST));
         } else if (animatedSubtitleTextView != null) {
             animatedSubtitleTextView.measure(MeasureSpec.makeMeasureSpec(availableWidth, MeasureSpec.EXACTLY), MeasureSpec.makeMeasureSpec(dp(20), MeasureSpec.AT_MOST));
         }
@@ -759,55 +788,117 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
         glassMode = true;
     }
 
+    private boolean isCenteredTitle() {
+        return desu.inugram.InuConfig.CENTER_TITLE_CHATS.getValue() && inu_isRealChatHeader();
+    }
+
+    private boolean inu_isRealChatHeader() {
+        return parentFragment != null
+                && parentFragment.getChatMode() != ChatActivity.MODE_SAVED
+                && !parentFragment.isReplyChatComment()
+                && parentFragment.getDialogId() != 0
+                && parentFragment.getDialogId() != UserConfig.getInstance(UserConfig.selectedAccount).getClientUserId()
+                && parentFragment.getDialogId() != UserObject.REPLY_BOT;
+    }
+
+    public boolean isRightAvatar() {
+        return glassMode && isCenteredTitle() && desu.inugram.InuConfig.CENTER_TITLE_RIGHT_AVATAR.getValue();
+    }
+
     @Override
     protected void onLayout(boolean changed, int left, int top, int right, int bottom) {
         final int actionBarHeight = ActionBar.getCurrentActionBarHeight();
         final int viewTop = (actionBarHeight - avatarImageView.getMeasuredHeight() - 2) / 2 + (occupyStatusBar ? AndroidUtilities.statusBarHeight : 0);
         final int subtitleTop = viewTop + dp(glassMode ? 23.66f : 24);
 
-        avatarImageView.layout(1 + leftPadding, 1 + viewTop, 1 + leftPadding + avatarImageView.getMeasuredWidth(), 1 + viewTop + avatarImageView.getMeasuredHeight());
-        int l = leftPadding + (avatarImageView.getVisibility() == VISIBLE ? dp(glassMode ? 49.66f : 55) : dp(glassMode ? 13 : 1)) + rightAvatarPadding;
+        boolean centerChatTitle = isCenteredTitle();
+        boolean rightAvatar = isRightAvatar();
+        final int avatarW = avatarImageView.getMeasuredWidth();
+        final int avatarH = avatarImageView.getMeasuredHeight();
+        ActionBar actionBar = parentFragment != null ? parentFragment.getActionBar() : null;
+        if (actionBar == null && getParent() instanceof ActionBar) {
+            actionBar = (ActionBar) getParent();
+        }
+
+        int textLeftBound;
+        int textRightBound;
+
+        if (rightAvatar) {
+            int pillWidth = (actionBar != null ? (actionBar.inu_glassTitleRight - actionBar.inu_glassTitleLeft) : 0);
+            if (pillWidth <= 0) {
+                pillWidth = getVisualWidth() + dp(12);
+            }
+            int avatarRight = pillWidth - (1 + leftPadding);
+            int avatarLeft = avatarRight - avatarW;
+            if (avatarLeft < dp(10)) {
+                avatarLeft = (right - left) - avatarW - (1 + leftPadding);
+            }
+            avatarImageView.setTranslationX(0);
+            avatarImageView.layout(avatarLeft, 1 + viewTop, avatarLeft + avatarW, 1 + viewTop + avatarH);
+
+            int inset = avatarImageView.getVisibility() == VISIBLE ? (1 + leftPadding + avatarW + dp(4)) : dp(glassMode ? 14 : 10);
+            textLeftBound = (centerChatTitle ? inset : dp(glassMode ? 14 : 10)) + rightAvatarPadding;
+            textRightBound = Math.max(textLeftBound, pillWidth - inset);
+        } else {
+            avatarImageView.setTranslationX(0);
+            avatarImageView.layout(1 + leftPadding, 1 + viewTop, 1 + leftPadding + avatarW, 1 + viewTop + avatarH);
+            int inset = avatarImageView.getVisibility() == VISIBLE ? (1 + leftPadding + avatarW + dp(4)) : dp(glassMode ? 20 : 16);
+            textLeftBound = (centerChatTitle ? inset : (1 + leftPadding + avatarW + dp(8))) + rightAvatarPadding;
+            textRightBound = (right - left) - (centerChatTitle ? inset : dp(glassMode ? 20 : 16));
+        }
+        int availableWidth = Math.max(0, textRightBound - textLeftBound);
+
+        int l = textLeftBound;
+        int r = centerChatTitle ? (textLeftBound + availableWidth) : (textLeftBound + titleTextView.getMeasuredWidth());
         SimpleTextView titleTextLargerCopyView = this.titleTextLargerCopyView.get();
         if (getSubtitleTextView().getVisibility() != GONE) {
-            titleTextView.layout(l, viewTop + dp(1.66f) - titleTextView.getPaddingTop(), l + titleTextView.getMeasuredWidth(), viewTop + titleTextView.getTextHeight() + dp(1.66f) - titleTextView.getPaddingTop() + titleTextView.getPaddingBottom());
+            titleTextView.layout(l, viewTop + dp(1.66f) - titleTextView.getPaddingTop(), r, viewTop + titleTextView.getTextHeight() + dp(1.66f) - titleTextView.getPaddingTop() + titleTextView.getPaddingBottom());
             if (titleTextLargerCopyView != null) {
-                titleTextLargerCopyView.layout(l, viewTop + dp(1.66f), l + titleTextLargerCopyView.getMeasuredWidth(), viewTop + titleTextLargerCopyView.getTextHeight() + dp(1.66f));
+                titleTextLargerCopyView.layout(l, viewTop + dp(1.66f), r, viewTop + titleTextLargerCopyView.getTextHeight() + dp(1.66f));
             }
         } else {
-            titleTextView.layout(l, viewTop + dp(10) - titleTextView.getPaddingTop(), l + titleTextView.getMeasuredWidth(), viewTop + titleTextView.getTextHeight() + dp(10) - titleTextView.getPaddingTop() + titleTextView.getPaddingBottom());
+            titleTextView.layout(l, viewTop + dp(10) - titleTextView.getPaddingTop(), r, viewTop + titleTextView.getTextHeight() + dp(10) - titleTextView.getPaddingTop() + titleTextView.getPaddingBottom());
             if (titleTextLargerCopyView != null) {
-                titleTextLargerCopyView.layout(l, viewTop + dp(10), l + titleTextLargerCopyView.getMeasuredWidth(), viewTop + titleTextLargerCopyView.getTextHeight() + dp(10));
+                titleTextLargerCopyView.layout(l, viewTop + dp(10), r, viewTop + titleTextLargerCopyView.getTextHeight() + dp(10));
             }
         }
+        int avatarLeftPos = avatarImageView.getLeft();
+        float transX = 0;
         if (communityItem != null) {
+            communityItem.setTranslationX(transX);
             communityItem.layout(
-                leftPadding + dp(29f),
+                avatarLeftPos + dp(28f),
                 viewTop + dp(27.33f),
-                leftPadding + dp(29f) + communityItem.getMeasuredWidth(),
+                avatarLeftPos + dp(28f) + communityItem.getMeasuredWidth(),
                 viewTop + dp(27.33f) + communityItem.getMeasuredHeight());
         }
         if (timeItem != null) {
+            timeItem.setTranslationX(transX);
             timeItem.layout(
-                leftPadding + dp(19.333f),
+                avatarLeftPos + dp(19.333f),
                 viewTop - dp(8),
-                leftPadding + dp(19.333f) + timeItem.getMeasuredWidth(),
+                avatarLeftPos + dp(19.333f) + timeItem.getMeasuredWidth(),
                 viewTop - dp(8) + timeItem.getMeasuredHeight()
             );
         }
         if (starBgItem != null) {
-            starBgItem.layout(leftPadding + dp(28), viewTop + dp(24), leftPadding + dp(28) + starBgItem.getMeasuredWidth(), viewTop + dp(24) + starBgItem.getMeasuredHeight());
+            starBgItem.setTranslationX(transX);
+            starBgItem.layout(avatarLeftPos + dp(28), viewTop + dp(24), avatarLeftPos + dp(28) + starBgItem.getMeasuredWidth(), viewTop + dp(24) + starBgItem.getMeasuredHeight());
         }
         if (starFgItem != null) {
-            starFgItem.layout(leftPadding + dp(28), viewTop + dp(24), leftPadding + dp(28) + starFgItem.getMeasuredWidth(), viewTop + dp(24) + starFgItem.getMeasuredHeight());
+            starFgItem.setTranslationX(transX);
+            starFgItem.layout(avatarLeftPos + dp(28), viewTop + dp(24), avatarLeftPos + dp(28) + starFgItem.getMeasuredWidth(), viewTop + dp(24) + starFgItem.getMeasuredHeight());
         }
+
+        int subR = centerChatTitle ? (textLeftBound + availableWidth) : (textLeftBound + (subtitleTextView != null ? subtitleTextView.getMeasuredWidth() : (animatedSubtitleTextView != null ? animatedSubtitleTextView.getMeasuredWidth() : 0)));
         if (subtitleTextView != null) {
-            subtitleTextView.layout(l, subtitleTop, l + subtitleTextView.getMeasuredWidth(), subtitleTop + subtitleTextView.getTextHeight());
+            subtitleTextView.layout(textLeftBound, subtitleTop, subR, subtitleTop + subtitleTextView.getTextHeight());
         } else if (animatedSubtitleTextView != null) {
-            animatedSubtitleTextView.layout(l, subtitleTop, l + animatedSubtitleTextView.getMeasuredWidth(), subtitleTop + animatedSubtitleTextView.getTextHeight());
+            animatedSubtitleTextView.layout(textLeftBound, subtitleTop, subR, subtitleTop + animatedSubtitleTextView.getTextHeight());
         }
         SimpleTextView subtitleTextLargerCopyView = this.subtitleTextLargerCopyView.get();
         if (subtitleTextLargerCopyView != null) {
-            subtitleTextLargerCopyView.layout(l, subtitleTop, l + subtitleTextLargerCopyView.getMeasuredWidth(), subtitleTop + subtitleTextLargerCopyView.getTextHeight());
+            subtitleTextLargerCopyView.layout(textLeftBound, subtitleTop, subR, subtitleTop + subtitleTextLargerCopyView.getTextHeight());
         }
     }
 
@@ -909,10 +1000,11 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
         if (!rightDrawableIsScamOrVerified && !rightDrawableIsScam) {
             if (mutedIcon != null) {
                 rightDrawable2ContentDescription = getString(R.string.NotificationsMuted);
-            } else {
+                titleTextView.setRightDrawable2(mutedIcon);
+            } else if (!desu.inugram.helpers.DevBadgesHelper.isBadgeDrawable(titleTextView.getRightDrawable2())) {
                 rightDrawable2ContentDescription = null;
+                titleTextView.setRightDrawable2(null);
             }
-            titleTextView.setRightDrawable2(mutedIcon);
         }
         checkActionBar(true);
     }
@@ -985,10 +1077,14 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
         }
         if (parentFragment != null) {
             long dialogId = parentFragment.getDialogId();
+            boolean hasStatus = premium || DialogObject.getEmojiStatusDocumentId(emojiStatus) != 0;
+            boolean hasVerified = verified || scam || fake;
             desu.inugram.helpers.DevBadgesHelper.applyRightBadge(
                 titleTextView,
                 dialogId > 0 ? dialogId : 0L,
                 dialogId < 0 ? dialogId : 0L,
+                hasStatus,
+                hasVerified,
                 desu.inugram.helpers.DevBadgesHelper.HEADER_BADGE_SIZE_DP
             );
         }
@@ -1531,6 +1627,7 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
     @Override
     protected void onAttachedToWindow() {
         super.onAttachedToWindow();
+        post(this::requestLayout);
         if (parentFragment != null) {
             NotificationCenter.getInstance(currentAccount).addObserver(this, NotificationCenter.didUpdateConnectionState);
             NotificationCenter.getGlobalInstance().addObserver(this, NotificationCenter.emojiLoaded);
@@ -1737,9 +1834,9 @@ public class ChatAvatarContainer extends FrameLayout implements FactorAnimator.T
             width = Math.max(width, subtitleTextView.getExactWidthIncludeDrawables());
         }
         if (hasVisibleAvatar()) {
-            width += dp(52 + 12);
+            width += dp(52 + 18);
         } else {
-            width += dp(30);
+            width += dp(40);
         }
         return (int) width;
     }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
@@ -3248,6 +3248,10 @@ public class DialogsActivity extends BaseFragment implements NotificationCenter.
         }
     }
 
+    public boolean inu_isMainDialogsScreen() {
+        return !onlySelect && folderId == 0 && communityId == 0 && initialDialogsType == DIALOGS_TYPE_DEFAULT;
+    }
+
     @Override
     public View createView(final Context context) {
         searching = false;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
@@ -11691,11 +11691,16 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                         showDialog(premiumPreviewBottomSheet);
                     });
                 }
+                boolean hasStatus = (user != null && DialogObject.getEmojiStatusDocumentId(user.emoji_status) != 0) || (user != null && MessagesController.getInstance(currentAccount).isPremiumUser(user));
+                boolean hasVerified = (user != null && (user.verified || user.scam || user.fake));
                 desu.inugram.helpers.DevBadgesHelper.applyRightBadge(
                     nameTextView[a],
                     user.id,
                     0L,
-                    a == 0 ? desu.inugram.helpers.DevBadgesHelper.HEADER_BADGE_SIZE_DP : desu.inugram.helpers.DevBadgesHelper.PROFILE_BADGE_SIZE_DP
+                    hasStatus,
+                    hasVerified,
+                    a == 0 ? desu.inugram.helpers.DevBadgesHelper.HEADER_BADGE_SIZE_DP : desu.inugram.helpers.DevBadgesHelper.PROFILE_BADGE_SIZE_DP,
+                    a == 1
                 );
             }
 
@@ -11956,11 +11961,16 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                 } else {
                     nameTextView[a].setLeftDrawable(null);
                 }
+                boolean hasStatus = currentChat != null && DialogObject.getEmojiStatusDocumentId(currentChat.emoji_status) != 0;
+                boolean hasVerified = currentChat != null && (currentChat.verified || currentChat.scam || currentChat.fake);
                 desu.inugram.helpers.DevBadgesHelper.applyRightBadge(
                     nameTextView[a],
                     0L,
                     chatId != 0 ? chatId : (currentChat != null ? currentChat.id : 0L),
-                    a == 0 ? desu.inugram.helpers.DevBadgesHelper.HEADER_BADGE_SIZE_DP : desu.inugram.helpers.DevBadgesHelper.PROFILE_BADGE_SIZE_DP
+                    hasStatus,
+                    hasVerified,
+                    a == 0 ? desu.inugram.helpers.DevBadgesHelper.HEADER_BADGE_SIZE_DP : desu.inugram.helpers.DevBadgesHelper.PROFILE_BADGE_SIZE_DP,
+                    a == 1
                 );
                 if (a == 0 && onlineTextOverride != null) {
                     onlineTextView[a].setText(onlineTextOverride);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Stories/DialogStoriesCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Stories/DialogStoriesCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Stories/DialogStoriesCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Stories/DialogStoriesCell.java
@@ -940,10 +940,16 @@ public class DialogStoriesCell extends FrameLayout implements NotificationCenter
             titleView.setTranslationY(bottomY + dp(14) - offset + dp(FAKE_TOP_PADDING) - dp(6) * subtitleOverlayContainer.getTotalVisibility());
             int cellWidth = dp(72);
             lastViewRight += -cellWidth + getAvatarRight(cellWidth, collapsedProgress) + dp(12);
-            titleView.setTranslationX(lastViewRight);
-            titleView.getDrawable().setRightPadding(lastViewRight - dp(12) + actionBar.menu.getVisibleItemsMeasuredWidthWithAlpha() * progress);
-
-            telegramLogoView.setTranslationX(titleView.getTranslationX() + dp(1));
+            if (desu.inugram.InuConfig.CENTER_TITLE_MAIN.getValue()) {
+                float titleX = (getMeasuredWidth() - titleView.getDrawable().getCurrentWidth()) / 2f;
+                titleView.setTranslationX(titleX);
+                titleView.getDrawable().setRightPadding(0);
+                telegramLogoView.setTranslationX((getMeasuredWidth() - telegramLogoView.getMeasuredWidth()) / 2f);
+            } else {
+                titleView.setTranslationX(lastViewRight);
+                titleView.getDrawable().setRightPadding(lastViewRight - dp(12) + actionBar.menu.getVisibleItemsMeasuredWidthWithAlpha() * progress);
+                telegramLogoView.setTranslationX(titleView.getTranslationX() + dp(1));
+            }
             telegramLogoView.setTranslationY(bottomY + dp(14 + FAKE_TOP_PADDING + 4.333f) + translationOffset /*titleView.getTranslationY() + dpf2(37.33f)*/);
 
             if (desu.inugram.helpers.security.ParanoiaHelper.isDisguised()) {
