From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Oleksii Kulinich <entaytion@gmail.com>
Date: Sun, 16 Aug 2026 23:04:54 +0300
Subject: feat(history): preserve message history and saved deletions

- preserve deleted and edited message history behind existing settings
- capture outgoing edits before stock state changes
- render preserved deletions with the configured transparent style
---
 .../messenger/MessagesController.java         | 85 +++++++++++++++++--
 .../telegram/messenger/MessagesStorage.java   | 34 +++++++-
 .../messenger/SendMessagesHelper.java         |  7 ++
 .../telegram/ui/Cells/ChatMessageCell.java    |  8 +-
 .../java/org/telegram/ui/ChatActivity.java    | 19 +++++
 .../telegram/ui/Components/AlertsCreator.java | 44 +++++++++-
 .../Components/DeleteMessagesBottomSheet.java |  2 +
 .../org/telegram/ui/MainTabsActivity.java     | 11 +--
 8 files changed, 188 insertions(+), 22 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java
@@ -8114,16 +8114,20 @@ public class MessagesController extends BaseController implements NotificationCe
             settings.business_bot_manage_url = "https://telegram.org/";
         }
         editor.apply();
+        desu.inugram.helpers.chat.UserInfoSaverHelper.onSavePeerSettings(currentAccount, dialogId, settings);
         userPeerSettings.put(dialogId, settings);
         getNotificationCenter().postNotificationName(NotificationCenter.peerSettingsDidLoad, dialogId);
     }
 
     public TLRPC.PeerSettings getPeerSettings(long dialogId) {
         TLRPC.UserFull userFull = getUserFull(dialogId);
+        TLRPC.PeerSettings result;
         if (userFull != null && userFull.settings != null) {
-            return userFull.settings;
+            result = userFull.settings;
+        } else {
+            result = userPeerSettings.get(dialogId);
         }
-        return userPeerSettings.get(dialogId);
+        return desu.inugram.helpers.chat.UserInfoSaverHelper.onGetPeerSettings(currentAccount, dialogId, result);
     }
 
     public void loadPeerSettings(TLRPC.User currentUser, TLRPC.Chat currentChat) {
@@ -8305,7 +8309,19 @@ public class MessagesController extends BaseController implements NotificationCe
                 if (task != null) {
                     for (int a = 0, N = task.size(); a < N; a++) {
                         ArrayList<Integer> mids = task.valueAt(a);
-                        deleteMessages(mids, null, null, task.keyAt(a), 0, true, 0, !mids.isEmpty() && mids.get(0) > 0);
+                        long dialogId = task.keyAt(a);
+                        TLRPC.Dialog dialog = dialogs_dict.get(dialogId);
+                        if (desu.inugram.helpers.security.SelfDestructHelper.shouldPreserveMessage(dialogId, dialog != null ? dialog.ttl_period : 0)) {
+                            for (int i = 0; i < mids.size(); i++) {
+                                int mid = mids.get(i);
+                                MessageObject msgObj = dialogMessagesByIds.get(mid);
+                                long fromId = msgObj != null && msgObj.messageOwner != null && msgObj.messageOwner.from_id != null ? org.telegram.messenger.DialogObject.getPeerDialogId(msgObj.messageOwner.from_id) : 0L;
+                                String text = msgObj != null && msgObj.messageOwner != null ? msgObj.messageOwner.message : "";
+                                TLRPC.Message msg = msgObj != null ? msgObj.messageOwner : null;
+                                desu.inugram.helpers.chat.SavedMessagesHelper.markMessageDeleted(currentAccount, dialogId, mid, fromId, text, 0, msg, true);
+                            }
+                        }
+                        deleteMessages(mids, null, null, dialogId, 0, true, 0, !mids.isEmpty() && mids.get(0) > 0);
                     }
                 }
                 if (taskMedia != null) {
@@ -8315,12 +8331,14 @@ public class MessagesController extends BaseController implements NotificationCe
                         long dialogId = taskMedia.keyAt(a);
                         ArrayList<Integer> mids = taskMedia.valueAt(a);
                         if (checkViewer && viewerObject != null && viewerObject.currentAccount == currentAccount && viewerObject.getDialogId() == dialogId && mids.contains(viewerObject.getId())) {
-                            final int id = viewerObject.getId();
-                            mids.remove((Integer) id);
-                            viewerObject.forceExpired = true;
-                            final long taskId = createDeleteShowOnceTask(dialogId, id);
-                            SecretMediaViewer.getInstance().setOnClose(() -> doDeleteShowOnceTask(taskId, dialogId, id));
-                            getNotificationCenter().postNotificationName(NotificationCenter.updateMessageMedia, viewerObject.messageOwner);
+                            if (!desu.inugram.helpers.security.SelfDestructHelper.shouldPreserveMedia(dialogId)) {
+                                final int id = viewerObject.getId();
+                                mids.remove((Integer) id);
+                                viewerObject.forceExpired = true;
+                                final long taskId = createDeleteShowOnceTask(dialogId, id);
+                                SecretMediaViewer.getInstance().setOnClose(() -> doDeleteShowOnceTask(taskId, dialogId, id));
+                                getNotificationCenter().postNotificationName(NotificationCenter.updateMessageMedia, viewerObject.messageOwner);
+                            }
                         }
                         if (!mids.isEmpty()) {
                             getMessagesStorage().emptyMessagesMedia(dialogId, mids);
@@ -9311,6 +9329,33 @@ public class MessagesController extends BaseController implements NotificationCe
     }
 
     public void markDialogMessageAsDeleted(long dialogId, ArrayList<Integer> messages) {
+        boolean hasPendingKeepLocal = messages != null && desu.inugram.helpers.chat.SavedMessagesHelper.hasPendingKeepLocal(dialogId, messages);
+        if (desu.inugram.InuConfig.SAVE_DELETED_MESSAGES.getValue() || hasPendingKeepLocal) {
+            if (messages != null) {
+                ArrayList<MessageObject> objs = dialogMessage.get(dialogId);
+                for (int a = 0; a < messages.size(); a++) {
+                    int id = messages.get(a);
+                    boolean forceSave = desu.inugram.helpers.chat.SavedMessagesHelper.consumeKeepLocalRequest(dialogId, id);
+                    MessageObject found = null;
+                    if (objs != null) {
+                        for (int i = 0; i < objs.size(); ++i) {
+                            MessageObject obj = objs.get(i);
+                            if (obj != null && obj.getId() == id) {
+                                found = obj;
+                                break;
+                            }
+                        }
+                    }
+                    if (found != null) {
+                        long fromId = found.messageOwner != null && found.messageOwner.from_id != null ? org.telegram.messenger.DialogObject.getPeerDialogId(found.messageOwner.from_id) : 0;
+                        String text = found.messageOwner != null ? found.messageOwner.message : "";
+                        desu.inugram.helpers.chat.SavedMessagesHelper.markMessageDeleted(currentAccount, dialogId, id, fromId, text, 0, found.messageOwner, forceSave);
+                    } else {
+                        desu.inugram.helpers.chat.SavedMessagesHelper.markMessageDeleted(currentAccount, dialogId, id, 0L, "", 0, null, forceSave);
+                    }
+                }
+            }
+        }
         ArrayList<MessageObject> objs = dialogMessage.get(dialogId);
         if (objs != null) {
             for (int i = 0; i < objs.size(); ++i) {
@@ -9385,6 +9430,7 @@ public class MessagesController extends BaseController implements NotificationCe
                 getMessagesStorage().markMessagesAsDeleted(dialogId, messages, true, false, ChatActivity.MODE_WELCOME_MESSAGES, topicId);
             } else {
                 if (channelId == 0) {
+                    markDialogMessageAsDeleted(dialogId, messages);
                     for (int a = 0; a < messages.size(); a++) {
                         Integer id = messages.get(a);
                         MessageObject obj = dialogMessagesByIds.get(id);
@@ -18696,6 +18742,7 @@ public class MessagesController extends BaseController implements NotificationCe
                         messagesArr = new ArrayList<>();
                     }
                     messagesArr.add(message);
+                    desu.inugram.helpers.chat.SavedMessagesHelper.rememberMessage(currentAccount, message.dialog_id, message.id, message.message, message.media != null, message.date, message.entities, message.media);
 
                     ConcurrentHashMap<Long, Integer> read_max = message.out ? dialogs_read_outbox_max : dialogs_read_inbox_max;
                     Integer value = read_max.get(message.dialog_id);
@@ -19546,6 +19593,8 @@ public class MessagesController extends BaseController implements NotificationCe
                 ImageLoader.saveMessageThumbs(message);
                 AndroidUtilities.runOnUIThread(()-> getSendMessagesHelper().onMessageEdited(message));
 
+                desu.inugram.helpers.chat.SavedMessagesHelper.handleMessageEdited(currentAccount, message, dialogMessagesByIds.get(message.id));
+
                 boolean isDialogCreated = createdDialogIds.contains(message.dialog_id);
                 MessageObject obj = new MessageObject(currentAccount, message, usersDict, chatsDict, isDialogCreated, isDialogCreated);
                 getTranslateController().invalidateTranslation(obj);
@@ -21145,6 +21194,10 @@ public class MessagesController extends BaseController implements NotificationCe
                     getNotificationCenter().postNotificationName(NotificationCenter.messagesReadContent, key, value);
                 }
             }
+            if (deletedMessagesFinal != null && deletedMessagesFinal.get(0) != null) {
+                // Let filterTimedDeletions run, but we don't need to replace the list since markMessageDeleted handles it
+                desu.inugram.helpers.security.SelfDestructHelper.filterTimedDeletions(currentAccount, deletedMessagesFinal.get(0), dialogMessagesByIds);
+            }
             if (deletedMessagesFinal != null) {
                 for (int a = 0, size = deletedMessagesFinal.size(); a < size; a++) {
                     long dialogId = deletedMessagesFinal.keyAt(a);
@@ -21269,6 +21322,20 @@ public class MessagesController extends BaseController implements NotificationCe
             for (int a = 0, size = deletedMessages.size(); a < size; a++) {
                 long key = deletedMessages.keyAt(a);
                 ArrayList<Integer> arrayList = deletedMessages.valueAt(a);
+                if (arrayList == null || arrayList.isEmpty()) {
+                    continue;
+                }
+                if (desu.inugram.InuConfig.SAVE_DELETED_MESSAGES.getValue() && arrayList != null) {
+                    for (int i = 0; i < arrayList.size(); i++) {
+                        int mid = arrayList.get(i);
+                        MessageObject found = dialogMessagesByIds.get(mid);
+                        long targetDialogId = key != 0 ? key : (found != null ? found.getDialogId() : 0);
+                        long fromId = found != null && found.messageOwner != null && found.messageOwner.from_id != null ? org.telegram.messenger.DialogObject.getPeerDialogId(found.messageOwner.from_id) : 0;
+                        String text = found != null && found.messageOwner != null ? found.messageOwner.message : "";
+                        TLRPC.Message msgOwner = found != null ? found.messageOwner : null;
+                        desu.inugram.helpers.chat.SavedMessagesHelper.markMessageDeleted(currentAccount, targetDialogId, mid, fromId, text, date, msgOwner);
+                    }
+                }
                 getMessagesStorage().getStorageQueue().postRunnable(() -> {
                     ArrayList<Long> dialogIds = getMessagesStorage().markMessagesAsDeleted(key, arrayList, false, true, 0, 0);
                     getMessagesStorage().updateDialogsWithDeletedMessages(key, -key, arrayList, dialogIds);
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesStorage.java b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesStorage.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesStorage.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesStorage.java
@@ -4355,6 +4355,9 @@ public class MessagesStorage extends BaseController {
         if (message == null) {
             return false;
         }
+        if (desu.inugram.InuConfig.SAVE_DELETED_MESSAGES.getValue() || desu.inugram.helpers.security.SelfDestructHelper.shouldPreserveMedia(MessageObject.getDialogId(message))) {
+            return false;
+        }
         int type = 0;
         long id = 0;
         TLRPC.Document document = MessageObject.getDocument(message);
@@ -4766,6 +4769,9 @@ public class MessagesStorage extends BaseController {
     }
 
     public void emptyMessagesMedia(long dialogId, ArrayList<Integer> mids) {
+        if (desu.inugram.helpers.security.SelfDestructHelper.shouldPreserveMedia(dialogId)) {
+            return;
+        }
         storageQueue.postRunnable(() -> {
             SQLiteCursor cursor = null;
             SQLitePreparedStatement state = null;
@@ -14751,6 +14757,11 @@ public class MessagesStorage extends BaseController {
                 cursor.dispose();
                 cursor = null;
 
+                // messagesByDialogs is the only place a deletion's real dialog is known for a
+                // private chat: TL_updateDeleteMessages carries no peer, so the controller can only
+                // guess through dialogMessagesByIds (the dialog's newest message).
+                desu.inugram.helpers.chat.SavedMessagesHelper.markMessagesDeletedFromStorage(currentAccount, messagesByDialogs, deletedMessages);
+
                 ArrayList<TopicKey> topicsToDelete = null;
 
                 if (dialogId != 0) {
@@ -14972,8 +14983,12 @@ public class MessagesStorage extends BaseController {
                         cursor.dispose();
                         cursor = null;
                     }
-                    database.executeFast(String.format(Locale.US, "DELETE FROM messages_v2 WHERE mid IN(%s) AND uid = %d", ids, did)).stepThis().dispose();
-                    database.executeFast(String.format(Locale.US, "DELETE FROM messages_topics WHERE mid IN(%s) AND uid = %d", ids, did)).stepThis().dispose();
+                    if (!desu.inugram.InuConfig.SAVE_DELETED_MESSAGES.getValue()) {
+                        database.executeFast(String.format(Locale.US, "DELETE FROM messages_v2 WHERE mid IN(%s) AND uid = %d", ids, did)).stepThis().dispose();
+                        database.executeFast(String.format(Locale.US, "DELETE FROM messages_topics WHERE mid IN(%s) AND uid = %d", ids, did)).stepThis().dispose();
+                    } else {
+                        desu.inugram.helpers.chat.SavedMessagesHelper.deleteNonPreservedFromStorage(currentAccount, database, did, mids);
+                    }
                     database.executeFast(String.format(Locale.US, "DELETE FROM polls_v2 WHERE mid IN(%s) AND uid = %d", ids, did)).stepThis().dispose();
                     database.executeFast(String.format(Locale.US, "DELETE FROM bot_keyboard WHERE mid IN(%s) AND uid = %d", ids, did)).stepThis().dispose();
                     database.executeFast(String.format(Locale.US, "DELETE FROM bot_keyboard_topics WHERE mid IN(%s) AND uid = %d", ids, did)).stepThis().dispose();
@@ -16366,6 +16381,7 @@ public class MessagesStorage extends BaseController {
                                     TLRPC.Message oldMessage = TLRPC.Message.TLdeserialize(data, data.readInt32(false), false);
                                     oldMessage.readAttachPath(data, getUserConfig().clientUserId);
                                     data.reuse();
+                                    desu.inugram.helpers.chat.SavedMessagesHelper.recordEditHistoryIfChanged(currentAccount, MessageObject.getDialogId(message), message.id, oldMessage, message);
                                     if (reactionUpdates != null) {
                                         reactionUpdates.add(new SavedReactionsUpdate(selfId, oldMessage, message));
                                     }
@@ -16422,6 +16438,18 @@ public class MessagesStorage extends BaseController {
                             cursor.dispose();
                             cursor = null;
                             if (!exist) {
+                                if (desu.inugram.InuConfig.SAVE_EDITED_MESSAGES.getValue()) {
+                                    MessageObject inu_fallbackCached = getMessagesController().dialogMessagesByIds.get(message.id);
+                                    if (inu_fallbackCached != null && inu_fallbackCached.messageOwner != null) {
+                                        String inu_fallbackText = inu_fallbackCached.messageOwner.message != null ? inu_fallbackCached.messageOwner.message : "";
+                                        desu.inugram.helpers.chat.SavedMessagesHelper.recordEditHistory(currentAccount, MessageObject.getDialogId(message), message.id, inu_fallbackText, inu_fallbackCached.messageOwner.date, inu_fallbackCached.messageOwner);
+                                    } else {
+                                        // dialogMessagesByIds only tracks each dialog's current preview
+                                        // message; fall back to the shadow cache for any other message
+                                        // in a chat the user hasn't opened.
+                                        desu.inugram.helpers.chat.SavedMessagesHelper.recordEditHistoryFromShadow(currentAccount, MessageObject.getDialogId(message), message.id, message.message, message.media != null);
+                                    }
+                                }
                                 continue;
                             }
                         }
@@ -19101,7 +19129,7 @@ public class MessagesStorage extends BaseController {
         return (value & type) != 0;
     }
 
-    private void isForumCacheInvalidate(long dialogId) {
+    public void isForumCacheInvalidate(long dialogId) {
         dialogIsForumTyped.delete(dialogId);
     }
 
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java b/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java
@@ -1700,6 +1700,7 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
             } else if (scheduled) {
                 mode = ChatActivity.MODE_SCHEDULED;
             }
+            desu.inugram.helpers.chat.SavedMessagesHelper.requestPermanentDelete(dialogId, messageIds);
             getMessagesController().deleteMessages(messageIds, null, null, dialogId, topicId, false, mode);
         }
     }
@@ -2952,6 +2953,9 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
                 messageObject.editingMessageEntities = newMsg.entities;
                 path = newMsg.attachPath;
             } else {
+                if (desu.inugram.InuConfig.SAVE_EDITED_MESSAGES.getValue() && newMsg != null && newMsg.message != null && !android.text.TextUtils.isEmpty(newMsg.message)) {
+                    desu.inugram.helpers.chat.SavedMessagesHelper.recordEditHistory(currentAccount, messageObject.getDialogId(), messageObject.getId(), messageObject.messageOwner.message, messageObject.messageOwner.edit_date != 0 ? messageObject.messageOwner.edit_date : messageObject.messageOwner.date, messageObject.messageOwner);
+                }
                 messageObject.previousMedia = newMsg.media;
                 messageObject.previousMessage = newMsg.message;
                 messageObject.previousMessageEntities = newMsg.entities;
@@ -3473,6 +3477,9 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
                 req.flags |= TLObject.FLAG_18;
             }
         }
+        if (desu.inugram.InuConfig.SAVE_EDITED_MESSAGES.getValue() && messageObject != null && messageObject.messageOwner != null && messageObject.messageOwner.message != null && !android.text.TextUtils.isEmpty(messageObject.messageOwner.message) && !messageObject.messageOwner.message.equals(message)) {
+            desu.inugram.helpers.chat.SavedMessagesHelper.recordEditHistory(currentAccount, messageObject.getDialogId(), messageObject.getId(), messageObject.messageOwner.message, messageObject.messageOwner.edit_date != 0 ? messageObject.messageOwner.edit_date : messageObject.messageOwner.date, messageObject.messageOwner);
+        }
         return getConnectionsManager().sendRequest(req, (response, error) -> {
             if (error == null) {
                 getMessagesController().processUpdates((TLRPC.Updates) response, false);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
@@ -4187,7 +4187,7 @@ public class ChatMessageCell extends BaseCell implements SeekBar.SeekBarDelegate
     }
 
     private boolean checkDateMotionEvent(MotionEvent event) {
-        if (!currentMessageObject.isImportedForward()) {
+        if (!currentMessageObject.isImportedForward() && !desu.inugram.helpers.chat.ChatHelper.canClickTime(currentMessageObject)) {
             return false;
         }
         int x = (int) getEventX(event);
@@ -4205,7 +4205,9 @@ public class ChatMessageCell extends BaseCell implements SeekBar.SeekBarDelegate
                 if (timePressed) {
                     timePressed = false;
                     playSoundEffect(SoundEffectConstants.CLICK);
-                    delegate.didPressTime(this);
+                    if (!desu.inugram.helpers.chat.ChatHelper.onTimeClick(this) && delegate != null) {
+                        delegate.didPressTime(this);
+                    }
                     invalidate();
                     result = true;
                 }
@@ -6906,6 +6908,7 @@ public class ChatMessageCell extends BaseCell implements SeekBar.SeekBarDelegate
                 currentMessageObject.richLayout.detach(this);
             }
             currentMessageObject = messageObject;
+            setAlpha(desu.inugram.helpers.chat.ChatHelper.deletedAlpha(currentMessageObject));
             currentMessagesGroup = groupedMessages;
             wasAllChats = isAllChats;
             lastTime = -2;
@@ -28145,6 +28148,7 @@ public class ChatMessageCell extends BaseCell implements SeekBar.SeekBarDelegate
 
     @Override
     public void setAlpha(float alpha) {
+        alpha = Math.min(alpha, desu.inugram.helpers.chat.ChatHelper.deletedAlpha(currentMessageObject));
         if ((alpha == 1f) != (getAlpha() == 1)) {
             invalidate();
         }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -26580,6 +26580,12 @@ public class ChatActivity extends BaseFragment implements
                     }
                 }
                 obj.deleted = true;
+                // Decided per message, not per dialog: an own message with SAVE_DELETED_OWN off
+                // must vanish normally, and one the user deletes a second time is being purged from
+                // the archive, so the ghost has to disappear with it. Asking whether the archive
+                // already holds it would lose the race -- for a private chat the deletion is only
+                // recorded once MessagesStorage resolves the dialog off the storage queue.
+                boolean inu_keepVisible = desu.inugram.helpers.chat.SavedMessagesHelper.isPreservedOrWillBe(currentAccount, obj);
                 if (obj.scheduled && sent) {
                     obj.scheduledSent = true;
                 }
@@ -26588,6 +26594,18 @@ public class ChatActivity extends BaseFragment implements
                 }
                 int index = chatAdapter != null && chatAdapter.isFiltered && filteredMessagesDict != null ? chatAdapter.filteredMessages.indexOf(filteredMessagesDict.get(mid)) : messages.indexOf(obj);
                 if (index != -1) {
+                    if (inu_keepVisible) {
+                        // Keep message in chat as semi-transparent with deleted icon;
+                        // deselect if selected, then trigger redraw
+                        if (selectedMessagesIds[loadIndex].indexOfKey(mid) >= 0) {
+                            updatedSelected = true;
+                            addToSelectedMessages(obj, false, updatedSelectedLast = (a == size - 1));
+                        }
+                        if (chatAdapter != null && !chatAdapter.isFrozen) {
+                            chatAdapter.notifyItemChanged(chatAdapter.messagesStartRow + index);
+                        }
+                        updated = true;
+                    } else {
                     if (obj.scheduled) {
                         scheduledMessagesCount--;
                         updateScheduled = true;
@@ -26695,6 +26713,7 @@ public class ChatActivity extends BaseFragment implements
                         }
                     }
                     updated = true;
+                    }
                 }
             }
         }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/AlertsCreator.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/AlertsCreator.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/AlertsCreator.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/AlertsCreator.java
@@ -7925,6 +7925,9 @@ public class AlertsCreator {
         }
 
         final boolean[] deleteForAll = new boolean[1];
+        final boolean[] keepLocal = new boolean[1];
+        final boolean[] deletePermanently = new boolean[1];
+        FrameLayout inu_alertBox = null;
         boolean canRevokeInbox = user != null && MessagesController.getInstance(currentAccount).canRevokePmInbox;
         int revokeTimeLimit;
         if (user != null) {
@@ -8076,7 +8079,20 @@ public class AlertsCreator {
                 if (desu.inugram.InuConfig.DELETE_FOR_BOTH_MESSAGES.getValue()) {
                     cell.setChecked(deleteForAll[0] = true, false);
                 }
+                if (desu.inugram.InuConfig.PROMPT_KEEP_LOCAL_ON_DELETE.getValue() && !desu.inugram.helpers.chat.SavedMessagesHelper.isSaveDeletedEnabled()) {
+                    CheckBoxCell keepLocalCell = new CheckBoxCell(activity, 1, resourcesProvider);
+                    keepLocalCell.setBackgroundDrawable(Theme.getSelectorDrawable(false));
+                    keepLocalCell.setText(LocaleController.getString(R.string.InuKeepLocalCopy), "", false, false);
+                    keepLocalCell.setPadding(LocaleController.isRTL ? dp(16) : dp(8), 0, LocaleController.isRTL ? dp(8) : dp(16), 0);
+                    frameLayout.addView(keepLocalCell, LayoutHelper.createFrame(LayoutHelper.MATCH_PARENT, 48, Gravity.TOP | Gravity.LEFT, 0, 48, 0, 0));
+                    keepLocalCell.setOnClickListener(v -> {
+                        CheckBoxCell cell13 = (CheckBoxCell) v;
+                        keepLocal[0] = !keepLocal[0];
+                        cell13.setChecked(keepLocal[0], true);
+                    });
+                }
                 builder.setView(frameLayout);
+                inu_alertBox = frameLayout;
                 builder.setCustomViewOffset(9);
             }
         } else if (!scheduled && !isSavedMessages && !ChatObject.isChannel(chat) && encryptedChat == null) {
@@ -8138,11 +8154,26 @@ public class AlertsCreator {
                 if (desu.inugram.InuConfig.DELETE_FOR_BOTH_MESSAGES.getValue()) {
                     cell.setChecked(deleteForAll[0] = true, false);
                 }
+                if (desu.inugram.InuConfig.PROMPT_KEEP_LOCAL_ON_DELETE.getValue() && !desu.inugram.helpers.chat.SavedMessagesHelper.isSaveDeletedEnabled()) {
+                    CheckBoxCell keepLocalCell = new CheckBoxCell(activity, 1, resourcesProvider);
+                    keepLocalCell.setBackgroundDrawable(Theme.getSelectorDrawable(false));
+                    keepLocalCell.setText(LocaleController.getString(R.string.InuKeepLocalCopy), "", false, false);
+                    keepLocalCell.setPadding(LocaleController.isRTL ? dp(16) : dp(8), 0, LocaleController.isRTL ? dp(8) : dp(16), 0);
+                    frameLayout.addView(keepLocalCell, LayoutHelper.createFrame(LayoutHelper.MATCH_PARENT, 48, Gravity.TOP | Gravity.LEFT, 0, 48, 0, 0));
+                    keepLocalCell.setOnClickListener(v -> {
+                        CheckBoxCell cell13 = (CheckBoxCell) v;
+                        keepLocal[0] = !keepLocal[0];
+                        cell13.setChecked(keepLocal[0], true);
+                    });
+                }
                 builder.setView(frameLayout);
+                inu_alertBox = frameLayout;
                 builder.setCustomViewOffset(9);
             }
         }
 
+        desu.inugram.helpers.chat.SavedMessagesHelper.decorateDeleteAlert(builder, inu_alertBox, activity, resourcesProvider, currentAccount, selectedMessage, selectedMessages, selectedGroup, deletePermanently);
+
         AlertDialog.OnButtonClickListener deleteAction = (dialogInterface, i) -> {
             ArrayList<Integer> ids = null;
             long thisDialogId = dialogId;
@@ -8183,6 +8214,11 @@ public class AlertsCreator {
                 if (mergeDialogId != 0 && selectedMessage.messageOwner.peer_id != null && selectedMessage.messageOwner.peer_id.chat_id == -mergeDialogId) {
                     thisDialogId = mergeDialogId;
                 }
+                if (keepLocal[0]) {
+                    desu.inugram.helpers.chat.SavedMessagesHelper.requestKeepLocal(thisDialogId, ids);
+                }
+                desu.inugram.helpers.chat.SavedMessagesHelper.requestPermanentDelete(thisDialogId, ids);
+                desu.inugram.helpers.chat.SavedMessagesHelper.extractPreserved(currentAccount, thisDialogId, ids, deletePermanently[0], fragment);
                 if (!ids.isEmpty()) {
                     MessagesController.getInstance(currentAccount).deleteMessages(ids, random_ids, encryptedChat, thisDialogId, topicId, deleteForAll[0], mode);
                 }
@@ -8205,7 +8241,13 @@ public class AlertsCreator {
                             }
                         }
                     }
-                    MessagesController.getInstance(currentAccount).deleteMessages(ids, random_ids, encryptedChat, (a == 1 && mergeDialogId != 0) ? mergeDialogId : thisDialogId, topicId, deleteForAll[0], mode);
+                    long deleteDialogId = (a == 1 && mergeDialogId != 0) ? mergeDialogId : thisDialogId;
+                    if (keepLocal[0]) {
+                        desu.inugram.helpers.chat.SavedMessagesHelper.requestKeepLocal(deleteDialogId, ids);
+                    }
+                    desu.inugram.helpers.chat.SavedMessagesHelper.requestPermanentDelete(deleteDialogId, ids);
+                    desu.inugram.helpers.chat.SavedMessagesHelper.extractPreserved(currentAccount, deleteDialogId, ids, deletePermanently[0], fragment);
+                    MessagesController.getInstance(currentAccount).deleteMessages(ids, random_ids, encryptedChat, deleteDialogId, topicId, deleteForAll[0], mode);
                     selectedMessages[a].clear();
                 }
             }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/DeleteMessagesBottomSheet.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/DeleteMessagesBottomSheet.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/DeleteMessagesBottomSheet.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/DeleteMessagesBottomSheet.java
@@ -1117,6 +1117,8 @@ public class DeleteMessagesBottomSheet extends BottomSheetWithRecyclerListView {
                 });
             }
         } else {
+            desu.inugram.helpers.chat.SavedMessagesHelper.extractPreserved(currentAccount, -inChat.id, supergroupMessageIds, true, null);
+            desu.inugram.helpers.chat.SavedMessagesHelper.extractPreserved(currentAccount, mergeDialogId, groupMessageIds, true, null);
             if (!supergroupMessageIds.isEmpty()) {
                 MessagesController.getInstance(currentAccount).deleteMessages(supergroupMessageIds, null, null, -inChat.id, topicId, false, mode);
             }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/MainTabsActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/MainTabsActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/MainTabsActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/MainTabsActivity.java
@@ -103,12 +103,7 @@ public class MainTabsActivity extends ViewPagerActivity implements NotificationC
     private static final int INDEX_PROFILE = 4;
 
     private static int indexToPosition(int index) {
-        int position = index > 2 ? index - 1 : index;
-        if (MainTabsHelper.isContactsTabHidden()) {
-            if (position == POSITION_CONTACTS) return -1;
-            if (position > POSITION_CONTACTS) position--;
-        }
-        return position;
+        return MainTabsHelper.indexToPosition(index);
     }
 
     private static final int ANIMATOR_ID_TABS_VISIBLE = 0;
@@ -353,7 +348,9 @@ public class MainTabsActivity extends ViewPagerActivity implements NotificationC
             tab.setMainTabsCompact(compact);
         }
 
-        for (int index = 0; index < tabs.length; index++) {
+        final int[] inu_visualOrder = MainTabsHelper.visualOrder();
+        for (int inu_oi = 0; inu_oi < inu_visualOrder.length; inu_oi++) {
+            final int index = inu_visualOrder[inu_oi];
             final GlassTabView view = tabs[index];
 
             final int position = indexToPosition(index);
